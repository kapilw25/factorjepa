# vLLM + Qwen3-VL dependencies (installed in venv_vllm, NOT venv_walkindia)
# vllm installed separately via: uv pip install vllm --torch-backend=auto --extra-index-url https://wheels.vllm.ai/nightly
qwen-vl-utils>=0.0.14
transformers
accelerate
