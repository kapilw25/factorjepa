# =============================================================================
# Base Requirements (M1 Mac / CPU / API-based operations)
# =============================================================================
# Install: pip install -r requirements.txt
# =============================================================================

# Environment & Config
python-dotenv>=1.0.0
PyYAML>=6.0  # configs/pipeline.yaml, configs/pretrain/*.yaml

# HTTP & API
requests>=2.31.0
openai>=1.0.0
huggingface_hub>=0.20.0
datasets>=2.14.0

# Video Download
yt-dlp>=2024.1.0

# Scene Detection
scenedetect[opencv]>=0.6.0

# Image Processing
Pillow>=10.0.0

# Data & Plotting
numpy>=2.3.0,<2.4          # Pin >=2.3: cuML/CuPy requires numpy 2.x. SAM3 claims <2.0 but works fine.
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
scipy>=1.10.0

# UMAP Visualization
umap-learn>=0.5.0

# ML Metrics (silhouette score in m06)
scikit-learn>=1.3.0

# PDF Text Extraction
PyMuPDF>=1.24.0

# Progress Bar
tqdm>=4.65.0

# Linting (post-edit hook: catches undefined names, unused vars)
ruff>=0.15.0

# Live process stack sampler — diagnose silent stalls in long GPU runs without
# modifying code. Attach with `py-spy dump --pid <PID>` to dump Python stacks of
# a running process (e.g., m09b_explora hang during torch.compile recompile),
# or `py-spy top --pid <PID>` for live hotspot view. Static Rust binary, no deps.
py-spy>=0.4.0
