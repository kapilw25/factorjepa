# =============================================================================
# GPU Requirements (Linux + Nvidia GPU)
# =============================================================================
# Install: pip install -r requirements_gpu.txt
#
# Prerequisites:
#   - Linux (Ubuntu 20.04+)
#   - Nvidia GPU (A100-40GB+ recommended)
#   - CUDA 12.x
#   - PyTorch 2.5.1+cu124 (install separately via setup_env.sh --gpu)
#
# Modules covered:
#   - src/m03_vjepa_embed.py (V-JEPA 2 embeddings)
#   - src/m04_qwen_tag.py (Qwen3-VL tagging)
#   - src/m05_faiss_metrics.py (FAISS GPU + metrics)
# =============================================================================

# Base requirements (includes yt-dlp, scenedetect, umap-learn, etc.)
-r requirements.txt

# =============================================================================
# Transformers & Model Loading
# =============================================================================
transformers>=4.45.0
accelerate>=0.25.0
bitsandbytes>=0.41.0

# =============================================================================
# Qwen-VL specific
# =============================================================================
qwen-vl-utils>=0.0.2

# =============================================================================
# Video Processing
# =============================================================================
av>=12.0.0

# =============================================================================
# FAISS GPU (Similarity Search) - Nvidia GPU ONLY
# =============================================================================
# Install via setup_env.sh --gpu (no CPU fallback)
# faiss-gpu is installed separately due to CUDA version requirements
