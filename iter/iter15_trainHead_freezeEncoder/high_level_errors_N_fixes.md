# 🐛 Errors & Fixes — iter8 → iter11 (2026-04-12 → 2026-04-24)

> **STATUS (2026-04-24): 79 entries logged across iter8/iter9/iter10/iter11.** Recent (iter10/iter11): #71-#78 iter10 paired_eval resilience (partial-tolerance m05, fingerprinted ckpt paths, silent-factor-drop fail-loud, orchestrator brittle-queue cascade). iter11 v2 active: `prec_plateau_enabled`/`kill_switch_enabled`/`bwt_trigger_enabled` all disabled at yaml level (they operate below CI_half noise floor — `feedback_only_val_loss_early_stop.md`); shared `render_training_plots()` extracted to `utils/training.py`; `save_training_checkpoint(full=False)` now drops teacher+predictor+optimizer for disk-budget, with `load_training_checkpoint` graceful-skip on student-only ckpts.
>
> Recurring fix patterns: (1) SAM 3.1 multiplex needs `text + boxes` hybrid for tracking (#27); (2) SAM3 `--no-deps` install drops runtime deps → declare in requirements_gpu.txt (#5/#6/#7/#18/#19); (3) HF Hub HEAD timeouts → pre-cache via `hf download` in setup_env_uv.sh (#22); (4) thresholds calibrated for old noisy SAM3-text are wrong for new tight Grounded-SAM masks (#26); (5) single-anchor SAM3 tracking drifts → multi-anchor DINO re-seed every 4 frames (#32); (6) defensive guards around SAM3 multiplex: box clamp (#28), empty add_prompt (#30), state inconsistency (#31); (7) `verify_or_skip` must check completeness for partial-resume to work (#29); (8) **patches to `deps/vjepa2/` must be re-applied on every fresh clone via idempotent sed/Python step in setup_env_uv.sh (#59)** — otherwise #44-class bugs silently return on new GPU instances; (9) **SANITY-only 24 GB memory savers (PagedAdamW8bit + gradient_checkpointing + mode-gated yaml #56/#57/#58) exist for code-correctness tier only — POC/FULL must use clean fp32 AdamW on 96 GB for research-quality Prec@K comparisons, and 96 GB often removes the need for the savers entirely**.

## Environment Setup

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 1 | `setup_env_uv.sh` crashes: `.env: line 34: heud: command not found` | `GMAIL_APP_PASSWORD=vtjn heud iupn wrlc` unquoted spaces — bash executes `heud` as command | Quote the value: `"vtjn heud iupn wrlc"` | `.env` |
| 2 | `git_pull.sh` skips HF download: `HF_TOKEN not found` | Ran `git_pull.sh` before `setup_env_uv.sh` — no venv, no `python-dotenv`, `os.getenv` returns None | Swap order in runbook: setup_env first, then git_pull | `runbook.md` |
| 3 | `hf_outputs.py` crashes: `ModuleNotFoundError: No module named 'utils'` | `from utils.progress import make_pbar` without `sys.path.insert` — fails when run as CLI script | Add `sys.path.insert(0, str(Path(__file__).resolve().parent.parent))` | `src/utils/hf_outputs.py` |
| 4 | `git_pull.sh` deletes log files mid-run | `git clean -fd` removes untracked files including `logs/*.log` being written by `tee` | Add `logs/*.log` to `.gitignore` + `git rm --cached logs/*.log` | `.gitignore` |
| 5 | SAM3 numpy downgrade breaks cuML: `numpy.core.multiarray failed to import` | `uv pip install sam3` pulls `numpy<2.0` (SAM3 metadata), but cuML/CuPy needs numpy 2.x | `--no-deps` on SAM3 install; pin `numpy>=2.3` in requirements.txt | `setup_env_uv.sh`, `requirements.txt` |
| 6 | SAM3 import fails: `No module named 'pycocotools'` | SAM3's import chain pulls training code (`coco_json_loaders.py`) at load time | Add `pycocotools>=2.0.0` to `requirements_gpu.txt` | `requirements_gpu.txt` |
| 7 | SAM3 import fails: `No module named 'einops'` | SAM3's `rope.py` uses `rearrange/repeat` from einops, not declared as dependency | Add `einops>=0.6.0` to `requirements_gpu.txt` | `requirements_gpu.txt` |

| 7b | Checkpoint download takes 23h+ (wget ~350 KB/s on 2.4 Gbps link), partial file passes `if [ ! -f ]` check → corrupted checkpoint | `wget` uses single HTTP connection; Facebook CDN throttles per-connection; size comment said ~8 GB but file is 28 GB | Use `aria2c -x 16 -s 16` (16 parallel connections) → 28 GB in 53s at 566 MiB/s; fix size comment to ~28 GB; fix step numbering /7→/8 | `setup_env_uv.sh` |
| 18 | SAM3 import fails: `No module named 'iopath'` (at `model_builder.py:12 from iopath.common.file_io import g_pathmgr`) | Same root cause as #5/#6/#7: SAM3 `--no-deps` install (to preserve numpy≥2.3 for cuML) drops undeclared runtime deps | Add `iopath>=0.1.9` to `requirements_gpu.txt`, re-run `./setup_env_uv.sh --gpu --from-wheels` (step [3/8] picks it up; also pulls transitive `portalocker==3.2.0`) | `requirements_gpu.txt` |
| 19 | SAM3 import fails: `No module named 'ftfy'` (at `model/tokenizer_ve.py:20 import ftfy`) | Same `--no-deps` pattern — SAM3's CLIP-derived tokenizer uses `ftfy` for text normalization, not declared | Add `ftfy>=6.0.0` to `requirements_gpu.txt`, re-run setup_env_uv.sh | `requirements_gpu.txt` |

## Step A: m10 SAM 3.1 Segmentation

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 8 | `build_sam3_multiplex_video_predictor() got unexpected keyword argument 'gpus_to_use'` | SAM 3.1 multiplex builder doesn't accept `gpus_to_use` (SAM 3.0 only) | Use `build_sam3_predictor(version="sam3.1")` unified entry point | `m10_sam_segment.py` |
| 9 | `ModuleNotFoundError: No module named 'flash_attn_interface'` | SAM 3.1 defaults to FA3 (`flash_attn_interface`), we have FA2 (`flash_attn`) | Pass `use_fa3=False` to `build_sam3_predictor()` | `m10_sam_segment.py` |
| 10 | SIGSEGV (exit 139) — silent crash, no traceback | `torchcodec` segfaults on TAR-extracted MP4 bytes | Disable torchcodec in `video_io.py`, use PyAV | `src/utils/video_io.py` |
| 11 | `iter_clips_parallel` misuse: `TypeError: '<' not supported between instances of 'dict' and 'int'` | `for example in iter_clips_parallel(...)` iterates over tuple `(queue, event, thread)`, not clips | Unpack: `clip_q, tar_stop, _reader = iter_clips_parallel(...)` + `clip_q.get()` loop | `m10_sam_segment.py`, `m11_factor_datasets.py` |
| 12 | `FATAL: clip has empty notable_objects` — crashes entire pipeline | Some clips (7%) have empty `notable_objects` in tags.json | Return `None` from `get_agent_prompts()`, skip clip in caller | `m10_sam_segment.py` |
| 13 | Mean concept recall = 0.22 (quality gate FAIL) | All objects comma-joined into one text prompt — SAM treats as single query, detects nothing | One `add_prompt` call per object category (Meta benchmark pattern) | `m10_sam_segment.py` |
| 14 | Process hangs after "Done" — never exits, holds 20GB VRAM | SAM3 spawns async frame-loading threads, no `shutdown()` method | `os._exit(0)` after cleanup + `try/except` wrapper at `__main__` with `os._exit(1)` for crashes | `m10_sam_segment.py` |
| 15 | `ValueError: shapes (384,384) (480,854)` in `frame_union \|= m` | SAM3 returns different mask resolutions per prompt category | Normalize all masks to first detected resolution via `_resize_mask()` | `m10_sam_segment.py` |
| 16 | Process hangs on unhandled exception (no traceback, no exit) | `os._exit` only at end of `main()`, not on crash path | Wrap `main()` in `try/except` at `__main__` with `os._exit(1)` | `m10_sam_segment.py` |

## Step B: m11 Factor Datasets

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 17 | `ValueError: shapes (2,480,854,1) (16,480,854,3)` in `make_layout_only` | SAM3 propagated only 2 frames but video has 16 — mask/video temporal mismatch | Nearest-neighbor temporal interpolation: `linspace(0, T_mask-1, T_vid)` + spatial resize if needed | `m11_factor_datasets.py` |

## Step A (Redux): Grounded-SAM Pivot (2026-04-14)

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 20 | m11 per-clip 2x2 grids show D_A skipped + D_I empty + D_L identical to Original for 10/15 SANITY clips | Upstream m10 failure: SAM 3.1 native text grounding fails on Indian street imagery. Bad clip case: `n_expected=3` (car, wires, signage), `n_agents=0`, `concept_recall=0.0`, `agent_pixel_ratio=1.2%` (below m11's 2% threshold → D_A skip). Also a pre-existing silent bug: tags.json `notable_objects` mixes agents (car) with layout (wires, signage) — feeding layout to SAM as "agents" contaminates D_A | Pivot to Grounded-SAM: Grounding DINO (`IDEA-Research/grounding-dino-base`) does open-vocab text→box detection; SAM 3.1 does box→mask refinement + propagation. New `segment_clip()` signature, new `load_grounding_dino()`, fixed 17-category agent taxonomy in `ch11_surgery.yaml` (replaces per-clip VLM prompts). Per-category SAM 3.1 sessions preserved for D_I obj_id ranges. `DINO_TO_TAG` alias map keeps concept_recall reporting against `tag_taxonomy.json`'s coarser vocab | `m10_sam_segment.py`, `configs/train/ch11_surgery.yaml` |
| 21 | m10 runs without `HF_TOKEN`, `HF_HOME`, `TRANSFORMERS_CACHE` env vars: model weights (~1.2 GB DINO + 28 GB SAM3 fallback) would write to `/root/.cache/huggingface/` on root partition instead of `/workspace/volume/hf_cache`; gated models (e.g., `facebook/sam3.1`) would 401; same root cause as #2 — script doesn't call `load_dotenv()` and shell didn't `source .env`. Symptom: `grep -n "load_dotenv\|HF_TOKEN" src/m10_sam_segment.py` returned 0 matches. | Missing early `.env` loading at script top. Must run BEFORE `from transformers import ...` (which reads `HF_HOME`/`TRANSFORMERS_CACHE` at import time to locate cache) | Added at top of `src/m10_sam_segment.py` (before imports that read env): `try: from dotenv import load_dotenv` / `except ImportError: load_dotenv = None` / `if load_dotenv is not None: load_dotenv(Path(__file__).resolve().parent.parent / ".env")`. Mirrors `src/utils/hf_outputs.py:30-32` pattern. Hook `fail-hard-research.sh` blocks `except: pass` — use explicit None assignment instead. | `src/m10_sam_segment.py` |
| 22 | `AutoProcessor.from_pretrained("IDEA-Research/grounding-dino-base")` fails with 15+ retries of `ReadTimeoutError: ... (read timeout=10)` on HEAD requests for `processor_config.json` / `preprocessor_config.json` / `video_preprocessor_config.json` | HF Hub HEAD-request timeout is hard-coded to 10 seconds. On slow/flaky networks this consistently fails. `hf_transfer` doesn't apply (HEAD is plain HTTP, not download). Not an auth issue — grounding-dino-base is public. | Three options (pick one): **(A recommended)** Pre-download out-of-band: `source .env && huggingface-cli download IDEA-Research/grounding-dino-base` — uses parallel/resumable download, populates `$HF_HOME`, then script's HEAD requests become local FS reads. **(B)** Bump timeouts: `export HF_HUB_ETAG_TIMEOUT=60 HF_HUB_DOWNLOAD_TIMEOUT=120` before invocation. **(C)** After one successful download, run with `HF_HUB_OFFLINE=1` to skip all HEAD checks. Do NOT just retry the script — 10s timeout will keep hitting. | runbook.md / shell env |
| 23 | `output_guard.py` noisily attempts HF download for a local absolute path: `GET .../factorjepa-outputs/tree/main/%2Fworkspace%2Ffactorjepa%2Foutputs%2Fsanity%2Fm10_sam_segment?recursive=True → 404 "does not exist on main"` | `verify_or_skip()` passes the full local Path object (`/workspace/factorjepa/outputs/...`) where the HF sync helper expects a repo-relative subfolder (`outputs/sanity/m10_sam_segment`). URL-encodes the leading `/workspace/factorjepa/` and queries HF with it. 404 is non-fatal — caller falls through to "outputs missing — will re-compute" — but pollutes logs and wastes an HF round-trip per script run | Non-blocking for m10, deferred. Fix when cleaning output_guard: before the HF query, compute `subfolder = str(output_dir.relative_to(project_root))` so we pass `outputs/sanity/m10_sam_segment` instead of the absolute path | `src/utils/output_guard.py` |
| 24 | `TypeError: GroundingDinoProcessor.post_process_grounded_object_detection() got an unexpected keyword argument 'box_threshold'` at `m10:172` → hard crash on first clip | transformers >=4.51.0 renamed the kwarg: `box_threshold` → `threshold` (docs: "Threshold to keep object detection predictions based on confidence score. defaults to 0.25"). Our `requirements_gpu.txt` pins `transformers>=4.57.0`, which uses the new name. Also in same release: `results["labels"]` now returns `int` class ids instead of strings — string labels moved to `results["text_labels"]` | Two renames in `detect_boxes_grounding_dino()` (m10:172): (a) kwarg `box_threshold=...` → `threshold=...`; (b) iterate `results["text_labels"]` instead of `results["labels"]`. Config key in ch11_surgery.yaml stays `box_threshold` (semantic, user-facing name) — only the processor kwarg changes. Verified via `inspect.signature(p.post_process_grounded_object_detection)` → `(outputs, input_ids, threshold=0.25, text_threshold=0.25, target_sizes=None, text_labels=None)` | `src/m10_sam_segment.py` |
| 25 | `AssertionError` at `sam3_multiplex_tracking.py:1707` (`assert (boxes_xywh is not None) == (box_labels is not None)`) → hard crash when passing DINO boxes to SAM 3.1 on first clip | Three format mismatches between DINO output and SAM 3.1 box-prompt API (discovered via `sam3_base_predictor.py:141-172` + `sam3_multiplex_tracking.py:1663-1720`): (a) **xyxy vs xywh** — DINO `post_process_grounded_object_detection` returns `[x_min, y_min, x_max, y_max]` absolute pixels, SAM 3.1 expects `[x, y, w, h]` format. (b) **Absolute vs normalized** — SAM 3.1 asserts `(boxes_xywh >= 0).all() and (boxes_xywh <= 1).all()`, so boxes must be divided by frame W/H. (c) **Missing `bounding_box_labels`** — kwarg is required when `bounding_boxes` is provided; list of ints, one per box (value=1 means positive-prompt, SAM1/2 convention) | In `segment_clip()` (m10:~295): replace `bounding_boxes=boxes` with `bounding_boxes=[[x0/W, y0/H, (x1-x0)/W, (y1-y0)/H] for x0,y0,x1,y1 in boxes]` and add `bounding_box_labels=[1]*len(boxes)`. Keep `W`/`H` from DINO output (already plumbed through `dino_out["H"]`/`dino_out["W"]`) | `src/m10_sam_segment.py` |
| 26 | SANITY v3 (first green run): pipeline completed 20/20 clips with mean mask confidence **0.934** and 65% clips with agents, BUT quality gate `pixel_ratio_min` FAILED (mean 0.5% < 2% threshold). 7/20 clips had 0 detections — overlay inspection confirmed those were **legit empty scenes** (monuments, deserted Goa lanes) — true negatives, not misses | Old `pixel_ratio_min=0.02` threshold was calibrated for the OLD noisy SAM3-text pipeline (avg 3-5% from FALSE POSITIVES on wires/signage). New Grounded-SAM pipeline produces tight precise masks averaging 0.5-1.0% per clip — distribution has fundamentally shifted. Same threshold applied at m11 (`min_agent_area_pct=0.02`) would skip D_A on 16/20 clips → starve Stage 2 surgery training. At 115K scale, losing 35% of clips to no-D_A/no-D_I is paper-killing | **Triple tune** (Option C — research-first, aggressive recall): (1) m10 quality gate `pixel_ratio_min: 0.02 → 0.002` (diagnostic match to new distribution); (2) m11 `min_agent_area_pct: 0.02 → 0.003` (accept clips with tight precise masks as D_A-valid); (3) DINO `box_threshold: 0.25 → 0.15` and `text_threshold: 0.20 → 0.12` to rescue the 7 zero-detection clips. FP risk bounded downstream by SAM3 `min_confidence=0.3` + `min_mask_area_pct=0.001` + fixed agent-only 17-category taxonomy (no layout class to hallucinate onto) | `configs/train/ch11_surgery.yaml`, `src/m10_sam_segment.py` |
| 27 | SANITY v4 (Option C): pipeline completes, m10 quality gate PASSES, but per-clip 2x2 verify grids show D_L identical to Original on all 20 clips, D_A is uniformly-dimmed (not agent-isolated), D_I empty everywhere. Centroid dump for FoDu market clip (30 detections): **28/30 objects tracked only on frame 0**, 2 sporadically on frames {0,5,6}; multi-agent frames = 3/16 (need ≥4 consecutive for D_I) | **Using SAM 3.1 backwards.** Per `sam3_multiplex_tracking.py:1700-1715`, SAM 3.1 multiplex was designed for `text_str` + (optional) `bounding_boxes` together: text propagates as cross-frame tracking signal (`find_inputs[t].text_ids` set for ALL frames), boxes only refine frame-0 segmentation. We had been passing **boxes-only, no text** → SAM3 has no "what to track" signal in frames 1-15 → drops most boxes after frame 0 → frame-0-only masks → m11 sees empty `agent_mask` for frames 1-15 → D_L = original, D_A = uniformly-dimmed (layout_mask=all-True at those frames), D_I = no co-occurring agents | **Path D — text + boxes hybrid.** In `segment_clip()` add_prompt call, pass `text=category` (the DINO-detected category name, e.g., "car") ALONGSIDE `bounding_boxes=` and `bounding_box_labels=`. Splits responsibilities: DINO=detect (open-vocab), SAM3 box=refine frame 0 (geometric precision), SAM3 text=track frames 1-15 (cross-frame propagation). 5-line change. Same compute cost as boxes-only (no extra forwards). Avoids original SAM3-text-only failure mode because boxes provide frame-0 anchor — text only needs to TRACK an already-identified object, not FIND it (much easier task). **VERIFIED on SANITY v5**: agent_pixel_ratio jumped 5-25× per active clip (FoDu: 1.5% → 9.7%, IQDGp: 3.1% → 15.2%); n_total_interactions: 0 → 39; D_I tubes: 0 → 39 across 9/20 clips; per-clip verify grids show clean D_L blur, D_A isolation, D_I cropped tubes | `src/m10_sam_segment.py` |
| 28 | `m10_dense100_v1` crashed at clip ~57/100 with `AssertionError` at `sam3_multiplex_tracking.py:1717` (`assert (boxes_xywh >= 0).all() and (boxes_xywh <= 1).all()`) — same assertion as #25 but different cause. SANITY v5 (20 easy-ish clips) never hit this; it only surfaced at scale on a dense-scene clip where DINO returned a box partially out-of-frame | DINO's `post_process_grounded_object_detection` can return boxes with coordinates slightly outside frame bounds for objects cut off at the edges (e.g., `x1 > W` for a truck exiting the right side, or `x0 < 0` for a partial pedestrian at the left edge). Our Path D normalization `[x0/W, y0/H, (x1-x0)/W, (y1-y0)/H]` then produces values `< 0` or `width > 1`, tripping SAM 3.1's bounds assertion. Unlike #25 (forgot to normalize at all), this is a data-domain issue (DINO can legitimately emit out-of-frame coords) | Clamp box coordinates to frame bounds BEFORE normalization, and drop degenerate (zero-area after clamp) boxes. If ALL boxes for a category were out-of-frame, close the SAM3 session and skip that category. `src/m10_sam_segment.py:~300`: replace the list-comprehension normalization with per-box clamp loop: `x0c=max(0, min(W, x0))`, `x1c=max(0, min(W, x1))`, `w=x1c-x0c`, skip if `w<=0 or h<=0` | `src/m10_sam_segment.py` |
| 29 | After #28 fix, m10 resume run v2 exited immediately with `[output_guard] m10 SAM3.1: ALL VALID — skipping (cached)` / `OK segments: 50 keys` — even though 50/100 clips remained unprocessed from the crash. m10 couldn't reach its built-in resume logic (which properly uses `.m10_checkpoint.json` + `iter_clips_parallel(processed_keys=...)`). | `verify_or_skip()` checked only that `segments.json` existed, not whether it had the FULL expected clip count. The `min_clips` parameter existed but only applied to `.npy` files (line 97 compares against `clip_count` set only in the `.npy` branch at line 61). JSON branch counted keys for display but didn't set `clip_count`, so the minimum check silently skipped | Two edits: (1) `src/utils/output_guard.py`: in the JSON branch, set `clip_count = len(data)` for both dict and list cases so `min_clips` check (line 97) fires correctly. (2) `src/m10_sam_segment.py`: compute `clip_limit` BEFORE `verify_or_skip` call, then pass `min_clips=clip_limit`. Now partial `segments.json` (50/100) correctly fails the completeness check, guard returns False, m10 proceeds to resume logic that loads `.m10_checkpoint.json` and skips done clips via `iter_clips_parallel(processed_keys=...)`. Fix benefits any module using `verify_or_skip` with a JSON output (m11, m06, ...) | `src/utils/output_guard.py`, `src/m10_sam_segment.py` |
| 30 | m10 dense100 v3 crashed at clip ~72/100 with `RuntimeError: No points are provided; please add points first` at `video_tracking_multiplex_demo.py:3410` — raised from `sam_predictor.handle_stream_request(type="propagate_in_video")` at `m10:350`. | Some specific clip/category combo: `add_prompt` with `text=category + bounding_boxes` accepted the prompt (no assertion) but produced **zero masks on frame 0** (`out0["out_obj_ids"]` was empty) — SAM3's text encoder couldn't ground that category on this particular frame AND/OR DINO's boxes didn't resolve to valid masks after SAM3's internal decoder filters. Then `cond_frame_outputs` was left empty, and the subsequent `propagate_in_video` check `if len(output_dict["cond_frame_outputs"]) == 0` raised. Failure surfaced late (clip 72) because it requires a specific clip+category where text grounding fails AND box-refinement yields zero masks — rare | Guard the propagation call: after `add_prompt`, check `len(out0.get("out_obj_ids", []))`; if zero, close the SAM3 session cleanly, increment `obj_id_offset += 100`, and `continue` to next category — do NOT call `propagate_in_video` with an empty prompt. `src/m10_sam_segment.py:~340` added `if n_frame0_objs == 0:` short-circuit | `src/m10_sam_segment.py` |
| 31 | m10 dense100 v4 crashed with **same RuntimeError as #30** but this time the `n_frame0_objs == 0` guard passed (out_obj_ids WAS non-empty). Crash happened on the 3rd category session of a clip: sessions 1 and 2 completed normally, session 3 crashed mid-propagation at `sam3_multiplex_base.py:1708` → `video_tracking_multiplex_demo.py:3410` | SAM 3.1 **internal state inconsistency**: the outer session's `out_obj_ids` reports N objects from add_prompt, but one of the individual trackers inside the multiplex model has an empty `cond_frame_outputs`. Deep inside `_propogate_tracker_one_frame_local_gpu`, that tracker's own propagate_in_video fires the "No points are provided" check and raises. We can't fix this upstream without patching SAM3. Frame-0 masks ARE already captured (we loop out0 before calling propagate), so a failed propagation degrades this category to "frame-0-only" rather than losing the clip entirely | Wrap the `for resp in handle_stream_request(propagate_in_video)` loop in `try/except RuntimeError`. Catch ONLY if `"No points are provided" in str(e)` (re-raise everything else, per `fail-hard-research.sh` hook — not a bare `except: pass`). On catch: print a WARN with the clip+category, keep the frame-0 masks we already stored, close the session, continue to next category. Graceful degradation: category goes from "full 16-frame tracking" to "frame-0 mask only" — still contributes a non-empty agent region for D_L/D_A, just no D_I-quality temporal consistency for THIS specific category on THIS clip | `src/m10_sam_segment.py` |
| 32 | m10 dense100 v5 COMPLETED 100/100 (no crashes), reported 1286 agents + 1759 interactions + 17.42% mean pixel ratio — metrics looked great. BUT manual eyeballing showed agents "hardly detected" across ~90% of overlay images. Diagnostic dump of raw masks revealed: (a) **frame 0 has clean, accurate silhouettes covering actual pedestrians/vehicles** (25-29% coverage on dense scenes), BUT (b) SAM 3.1 tracking **drifts heavily over frames**: by mid-frame, coverage drops 3-5× (e.g., 25.7% → 5.9% on Kolkata market clip). The `plot_overlay_per_clip()` overlay shows the MID frame (which has the weakest masks) with 40% alpha red blend (imperceptible on busy backgrounds). User was correct that detection visually looks bad — but the problem was tracking drift + viz choice, not wrong masks | Path D's single-anchor architecture (DINO on frame 0, SAM3 propagates 16 frames from that single seed) loses precision over time. SAM3's propagation is designed for short clips or object persistence; distant frames (12+ away from anchor) accumulate drift. Mid-frame coverage being ~5× worse than frame 0 is a real quality issue for D_L/D_A training — D_L's blur signal weakens on later frames, D_A's agent preservation misses agents that drifted | **Level 2 fix — multi-anchor DINO re-seed**: Run DINO on 4 evenly-spaced anchor frames `[0, 4, 8, 12]` for T=16. Each anchor "owns" its 4-frame segment (`[0-3], [4-7], [8-11], [12-15]`). Per anchor × per detected category: start a SAM 3.1 session, add_prompt with `frame_index=anchor` (not 0), propagate bidirectionally, but KEEP only the masks for frames in that anchor's segment. Max tracking drift per frame is now ≤2 frames instead of ≤15. Cost: 4× DINO forwards (~400ms overhead) + ~4× SAM 3.1 sessions per category (sessions are the bulk cost). Runtime on 100 dense clips: ~6 min → ~20-25 min. Quality win: fresh DINO detections every 4 frames → tight masks throughout every clip, no drift accumulation | `src/m10_sam_segment.py` |
| 33 | Attempted Level 2 speedup via `max_frame_num_to_track=3` to cut 16-frame bidirectional propagation to 4-frame each direction (~2.7× faster). v2 crashed on first new clip with `RuntimeError: The expanded size of the tensor (1) must match the existing size (0)... Tensor sizes: [5184, 0, 256]` at `video_tracking_multiplex.py:1307` (`current_vision_feats[-1].expand(-1, B, -1)` — empty batch dim). | **SAM 3.1 internal bug**: `propagate_in_video` computes `processing_order = range(start - 1, end - 1, -1)` for reverse direction. With anchor=0 and `max_frame_num_to_track=3`, end_frame_idx = -3, processing_order = [-1, -2, -3] — **invalid frame indices**. When `max_frame_num_to_track=None` (default 16), SAM3 has internal safeguards that filter invalid frames; with explicit `max=3` those safeguards don't trigger | Reverted to pure bidirectional. Superseded by #34 which solves both bug and slowdown via `propagation_direction="forward"` | `src/m10_sam_segment.py` |
| 34 | After #33 revert, m10 was correct but slow (~60s/clip = 100 min/100 clips, projected 17h for FULL 115K). Goal: speedup without triggering #33's bug. WebSearched SAM 2/3 issues — discovered SAM 3.1 exposes `propagation_direction` parameter via `handle_stream_request` (sam3_base_predictor.py:92) accepting `"forward"`, `"backward"`, or `"both"` (default) | Backward propagation was completely wasteful for our multi-anchor architecture: anchor=0 backward → invalid frames, anchors 4/8/12 backward → frames owned by previous anchor → discarded anyway | Pass `propagation_direction="forward"` + `max_frame_num_to_track=segment_size-1` to handle_stream_request. Theoretical 10× speedup, bit-identical output. **PARTIALLY VALIDATED, then crashed (see #35)** — direction works but max_frame_num_to_track triggers same #33 bug even with forward direction | `src/m10_sam_segment.py` |
| 35 | v4 with `propagation_direction="forward"` + `max_frame_num_to_track=3` crashed at clip ~51 with **same** `Tensor sizes: [5184, 0, 256]` error as #33. Forward propagation got to 75% (3/4 frames) before failing inside `_prepare_memory_conditioned_features`. So #34 was wrong about forward being safe with bounded max | The `max_frame_num_to_track` parameter is fundamentally unusable in SAM 3.1 multiplex — even forward direction triggers the bug. SAM3 internally does memory look-back / peek-ahead beyond the bounded iteration loop. When neighboring frames have empty vision features (because we capped propagation), `current_vision_feats[-1].expand(-1, B, -1)` accesses unloaded frame → empty batch dim → crash. The bug isn't about reverse direction specifically; it's about ANY bounded propagation in SAM 3.1's multiplex model | Drop `max_frame_num_to_track` entirely. Keep ONLY `propagation_direction="forward"` (which IS safe — it just disables the second propagation call, no bounded-frame issues). Bit-identical output (out-of-segment frames still discarded by `if fidx not in segment_frames`). Speedup: 32 frames per session → 16 frames per session = **~2× faster** (vs the dreamed 10× from #34). 100 dense clips: ~90 min → projected ~45 min. 115K FULL: 17h → ~8.5h. Acceptable for one-time research run | `src/m10_sam_segment.py` |
| 61 | 🕘 2026-04-17 (late): D.2 POC **v2 ran full 300 steps** (after #60 fix) but `m09_train_loss.png` showed loss dropping only 0.503 → 0.476 across 3 stages (∆ = −5.4 %). Investigation revealed `configs/train/ch11_surgery.yaml:158,167,176` set **warmup_steps: 200** per stage, while POC stage_steps = 99/99/102. Linear warmup from 0 → target LR over 200 steps = `(stage_end_step + 1) / 200 ≈ 50 %` of target LR AT BEST per stage. Every stage creates a fresh scheduler (m09c:454 new `LambdaLR`), so warmup restarts from 0 each stage → model ran at ≤50 % LR throughout. Loss barely moved because the optimizer barely moved. Final 0.476 is a warmup-truncated floor, not a converged value. | Hardcoded `warmup_steps: 200` was calibrated for FULL (thousands of steps/stage) where 200 is ~5 % of stage. At POC (99 steps/stage) it's 200% of stage → unreachable. Design didn't anticipate per-mode stage-step variation. | 🔧 Replaced fixed `warmup_steps` with a fraction `warmup_pct: 0.20` at `surgery:` level in ch11_surgery.yaml (auto-scales: POC 99 steps → 19 warmup; POC-1K 207 steps → 41 warmup; FULL 3593 steps → 718 warmup). Removed per-stage `warmup_steps` entries. m09c_surgery.py:417 computes `warmup_steps = max(1, int(stage_steps × surgery_cfg["warmup_pct"]))`. Gold-standard reference: Meta's base_optimization.yaml itself uses `warmup_cap_pct: 10` as a cap precisely to prevent this class of bug. Expected POC v3 behavior post-fix: loss drop ~0.10 per stage, final ~0.35-0.40. | `configs/train/ch11_surgery.yaml`, `src/m09c1_surgery_encoder.py` |
| 60 | 🕘 2026-04-17 (late): First D.2 POC run completed in 60 s with "SURGERY COMPLETE: 3 steps across 3 stages" — student_encoder.pt exported but runbook promised ~3 h. Investigation showed log line `Epochs: 100 | Steps: 300 | BS: 32` — but stage loop ran only 1 step per stage (3 total) because `configs/train/ch11_surgery.yaml:40` had **`max_epochs.poc: 1`** (yaml comment said "1 epoch per stage" but code interprets as "1 epoch TOTAL across all 3 stages"). `total_steps = steps_per_epoch × max_epochs = 3 × 1 = 3`, then `stage_steps = int(total_steps × stage_pct) = int(3 × 0.33) = 0` → `max(0, 1) = 1` per stage. 3 optimizer steps on a 2B model ≈ no-op. Exported student was 99 %+ identical to input frozen weights. Downstream Prec@K comparison would show "Surgery ≈ Frozen" and the cause would be invisible in the log. | Stale yaml comment ("1 epoch per stage for POC validation") that didn't match the code's interpretation of max_epochs. `runbook.md` said "~3h real training signal" assuming per-stage semantics. No fail-hard guard for "ran through but total_steps < threshold" — the #55 guard only catches 0-step cases, not 3-step silent near-no-ops. | 🔧 Bumped `max_epochs.poc: 1 → 100` in ch11_surgery.yaml (300 total steps → ~100 per stage). Also added `full: 3 → 300` for real FULL-tier training (was symmetric-stale). Yaml comment updated to match code: "1 epoch = 1 full pass × max_epochs total, split across stages via max_epochs_pct". Preflight suggestion: add a sanity check `assert total_steps ≥ 3 × batch_size / 10 for POC else WARN "training length suspiciously short"`. Not landed yet — manual for now. | `configs/train/ch11_surgery.yaml` |
| 59 | 2026-04-17: Fresh GPU instance (96 GB RTX Pro 6000 Blackwell) re-hit error #44 on the first `m05 --POC` run: `FATAL: Model load failed: RuntimeError when making fake tensor call ... F.scaled_dot_product_attention(q.dtype=float, k.dtype=float, v.dtype=c10::BFloat16)` at `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py:287`. The #44 patch from 2026-04-15 had been lost because `setup_env_uv.sh` line 223-231 does `rm -rf deps/vjepa2 && git clone --depth 1 facebookresearch/vjepa2` on every fresh provision — any local edit to `deps/vjepa2/**` is wiped. #44's documentation listed the fix file as `deps/vjepa2/...` but offered no reproducibility mechanism. Classic "fix applied on instance A, lost on instance B" class of bug. | #44 root cause (RoPE fp32 Q/K vs bf16 V dtype mismatch under torch.compile FakeTensor) still holds; this new entry is about the durability layer, not re-diagnosing. | Two-pronged durable fix. (a) Re-applied the 2-line cast `q = q.to(v.dtype); k = k.to(v.dtype)` at `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py` line 288-289 (between the Q/K concat at line 279-283 and the SDPA call at line 293) — unblocks current run. (b) Added an idempotent post-clone auto-patch step to `setup_env_uv.sh` right after the `git clone --depth 1 vjepa2` block: Python heredoc reads the file, skips if `q = q.to(v.dtype)` is already there ("patch already present"), else replaces the exact `"k = torch.cat([kd, kh, kw], dim=-1)\n\n        if self.use_sdpa:"` anchor with the cast lines inserted before `if self.use_sdpa:`. Raises `SystemExit("FATAL: vjepa2 upstream refactored ... #44 anchor not found")` if the anchor is missing — no silent skip on future vjepa2 refactors. Also checked sibling SDPA sites at modules.py:346 (standard Attention, Q/K/V all from one `nn.Linear` → same dtype, no RoPE) and :493 (cross-attention, K/V from `self.kv(x)`, Q from `self.q(q)`, both bf16 Linears → same dtype). Neither needs the cast. V-JEPA 2.0 Attention class in `deps/vjepa2/src/models/` intentionally NOT patched — legacy fallback path, add equivalent sed + file patch if 2.0 ablation ever re-runs. | `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py`, `setup_env_uv.sh` |
| 58 POSTSCRIPT 2026-04-17 | v8 teacher-CPU-offload patch was NEVER implemented. Instead, moved SANITY from 24GB RTX Pro 4000 to 96GB RTX Pro 6000 Blackwell. First run (2026-04-17) passed all 3 stages end-to-end: Stage 1 loss=0.4870, Stage 2=0.4901, **Stage 3=0.4806** (first ever successful Stage 3 completion). Stage 3 post-cleanup VRAM: 19.9 GB / 102 GB → 80 GB headroom. PagedAdamW8bit + gradient_checkpointing still active (mode-gated yaml SANITY=true) but now cosmetic overhead; the "v8 fallback ladder" (disable deep supervision, num_frames 16→8, predictor offload, sub-micro grad-accum) all stay on the shelf unused. **Lesson:** hardware migration can be a cheaper fix than code complexity — the 96GB instance costs ~$0.80/hr vs 24GB ~$0.20/hr, but the engineer-time saved by NOT landing v8 offload (~3h of code + test + maintenance) pays for many GPU-hours. For future memory-budget dilemmas in this project, compare "code fix" vs "hardware upgrade" as equal options, not hardware as last resort. | — | `src/m09c1_surgery_encoder.py` code untouched post-#58; only change was runtime invocation on 96GB. | — |
| 58 | After #57 landed, v6 24GB SANITY got through Stages 1+2 but fail-hard raised at `Stage stage3_interaction step 2: OOM persists at minimum sub-batch=1`. Analysis of corrected Stage 3 memory: student fp16 3.7 GB + teacher fp16 3.7 GB + predictor 0.12 GB + student trainable grads fp16 2.76 GB + **fp32 master weights 5.5 GB** (bnb.optim.AdamW8bit keeps these fp32, does NOT quantize master params — #56 underestimated) + 8-bit m1/m2 ~3 GB + activations (checkpointed) ~0.5 GB + CUDA context ~2 GB = ~21.3 GB steady-state → peak >24 GB at stage transitions. User explicitly rejected skipping Stage 3 on SANITY ("I need end-to-end testing >> complete validation / training loss graph so that when I spin up expensive 96GB GPU, I should not be wasting time in debugging") — accepts accuracy + throughput degradation in SANITY only | Two compounding memory issues fixable without moving to 96GB: **(A) Inter-stage optimizer double-hold** — `optimizer = build_optimizer(...)` evaluates RHS (allocates new Stage-3 state ~8.5 GB) BEFORE reassigning LHS (which would GC Stage-2's ~7.5 GB). During that window, both optimizers coexist on GPU → peak ~16 GB JUST for optimizer state, not 8.5 GB. Responsible for ~5-7 GB of the overshoot. **(B) Even steady-state fp32 master weights (5.5 GB) + 8-bit buffers crowd Stage 3 past 24 GB with no fragmentation slack.** HF QLoRA solves this exact problem for 7-65B fine-tuning on 24-48 GB via `bnb.optim.PagedAdamW8bit` — unified-memory CPU paging auto-swaps cold optimizer tensors to host RAM on pressure. Same 8-bit quantization as non-paged (zero accuracy change from current SANITY behavior), ~10-20% throughput hit from PCIe paging (acceptable per user's SANITY-only degradation allowance) | **Dual fix**: **(1) Explicit inter-stage cleanup** in `src/m09c1_surgery_encoder.py` at start of each stage iteration (after stage_idx=0): `optimizer = scheduler = sampler = None; gc.collect(); torch.cuda.empty_cache(); torch.cuda.ipc_collect()` — drops refs so Python GC releases state, then explicitly returns CUDA cache blocks to pool BEFORE the new build_optimizer call allocates. Prints `free/total` GB after cleanup for observability. Used None-assignment instead of `del` (lint-clean; same GC effect). **(2) PagedAdamW8bit for SANITY** — added `paged_optim` mode-gated yaml key ONLY in `configs/train/ch11_surgery.yaml` (not in base_optimization.yaml, to maintain scope discipline): `paged_optim: {sanity: true, poc: false, full: false}`. Flattened in m09c's `merge_config_with_args` alongside the other mode-gated flags. `build_optimizer` in `src/utils/training.py` reads `cfg_opt["paged_optim"]` ONLY inside the `if use_8bit:` branch — so m09a/m09b (which never hit use_8bit=True) never require the key, and m09c POC/FULL (use_8bit=false) also never read it. When both use_8bit=true AND paged_optim=true → `bnb.optim.PagedAdamW8bit` (verified constructs OK on bitsandbytes 0.49.2). Verified: lint clean, flattening returns `sanity: (8bit=T, paged=T, grad_ckpt=T); poc: (F, F, F); full: (F, F, F)`. Expected Stage 3 SANITY memory: ~13-16 GB on-GPU (optimizer state partially paged to CPU) + 8+ GB headroom for fragmentation. Throughput hit vs v6: inter-stage cleanup adds ~50ms per stage transition (negligible); paging adds ~10-20% per step (acceptable for SANITY code-validation purpose). POC/FULL on 96GB unchanged — still fp32 AdamW, no checkpointing, no paging → clean research-quality path | `src/m09c1_surgery_encoder.py`, `src/utils/training.py`, `configs/train/ch11_surgery.yaml` |
| 57 | After #56 landed, v6 SANITY Stage 1 succeeded (`loss=0.4841`, real number — within-step retry 4→2→1 worked, AdamW8bit + gradient checkpointing fit 24 GB). Asked whether 8-bit + checkpointing should extend to POC/FULL. User priority: (P1) Maximum accuracy, (P2) Maximum throughput. Goal: Surgery > Frozen on Prec@K with non-overlapping 95% CIs at POC (100 dense clips, 1 K validation). Dettmers et al. 2022 (arXiv:2110.02861) benchmarked 8-bit AdamW ≤1.3B (GPT-2, CLIP ViT-B/16, RoBERTa) reporting <1% accuracy vs fp32 — but V-JEPA ViT-G is 2B, extrapolation. For a Surgery > Frozen comparison where the expected Prec@K advantage may be ~1-5%, the 8-bit noise (~0.5% per paper's tail behavior) would widen CIs enough to threaten the non-overlap win condition. POC is a research-quality comparison and must run the published V-JEPA recipe (fp32 AdamW, no checkpointing) to eliminate optimizer as a confound | Splitting tiers preserves P1 without paying the full cost: SANITY runs on cheap 24 GB (8-bit + checkpointing) for code-correctness only; POC/FULL run on 96 GB RTX Pro 6000 Blackwell (fp32 AdamW, no checkpointing) for every number that ends up in the paper. CLAUDE.md explicitly designates SANITY for "code correctness, NOT model performance" so the 8-bit quarantine is principled, not ad-hoc. Cost delta for D.2 POC (~1.5 h on 24 GB vs ~1.0 h on 96 GB) = ~$0.50 — immaterial vs eliminating an accuracy confound in a NeurIPS target comparison | **Mode-gated yaml** overrides in `configs/train/ch11_surgery.yaml`: replaced scalar `use_8bit_optim: true` / `gradient_checkpointing: true` with per-mode dicts (`sanity: true, poc: false, full: false`). Same pattern as the pre-existing `max_epochs` mode-dict. `merge_config_with_args` in `src/m09c1_surgery_encoder.py` now flattens both new keys by `mode_key` (sanity/poc/full) right after the `max_epochs` flatten, so `build_optimizer` and the gradient-checkpointing call at line 315 read scalar values regardless of mode. Per CLAUDE.md fail-hard, no `.get()` — missing key crashes, forcing yaml to stay complete. Verified: `merge_config_with_args` with `SANITY=True` returns `{use_8bit_optim: True, gradient_checkpointing: True, max_epochs: 1}`; `POC=True` returns `{False, False, 1}`; `FULL=True` returns `{False, False, 3}`. Lint clean. m09a/m09b intentionally NOT extended per scope-discipline memory — their `use_8bit_optim`/`gradient_checkpointing` stay scalar `false` from `base_optimization.yaml` inheritance. Future: when m09b (ExPLoRA) or m09a (pretrain) need memory savings, extend similarly | `configs/train/ch11_surgery.yaml`, `src/m09c1_surgery_encoder.py` |
| 56 | After #55 fix, v5's fail-hard guard was predicted to surface the real memory ceiling: Stage 3 (36/48 trainable blocks, 1.38B trainable params) needs ~27 GB on a 24 GB Blackwell — 16.5 GB AdamW fp32 state (m1+m2+master) alone overflows, regardless of sub-batch size. Gradient checkpointing alone would save ~3-4 GB activations (insufficient); bitsandbytes AdamW8bit alone would save ~8 GB optimizer state (sufficient for Stage 3 but leaves activations tight at sub-batch>1). Combined A+B is the gold-standard path per HF Trainer's `optim="adamw_bnb_8bit" + gradient_checkpointing=True` for ViT-G fine-tuning on 24 GB consumer GPUs | (A) **Gradient checkpointing** via `torch.utils.checkpoint.checkpoint(fn, *args, use_reentrant=False)` — activations recomputed during backward instead of stored. Well-tested PyTorch primitive; HF `gradient_checkpointing_enable()` + FAIR vissl's `checkpoint_wrapper` both use the same pattern. `use_reentrant=False` is the modern API (torch.compile-compatible, proper kwarg handling for V-JEPA blocks which accept `mask`, `T`, `H_patches`, `W_patches`, `return_attn`, `mode`). Works on frozen blocks too (autograd still backprops through them to earlier trainable blocks, so frozen activations consume memory — checkpointing helps regardless of requires_grad). (B) **bitsandbytes AdamW8bit** (Dettmers et al. 2022, arXiv:2110.02861) — block-wise 8-bit quantization of AdamW m1/m2 buffers. Drop-in `torch.optim.AdamW` replacement. <1% accuracy loss on 80+ benchmarks in the paper. Already standard in HF Trainer and FAIR fairscale for 2B+ model fine-tuning on consumer GPUs | **Surgery-only wiring** (user-directed scope: focus on Surgery win condition, not m09a/m09b "for consistency"). Added two helpers to `src/utils/training.py`: (1) `enable_gradient_checkpointing(model)` — monkey-patches each block's forward with a closure that wraps `ckpt.checkpoint(original_forward, *args, use_reentrant=False, **kwargs)`. Idempotent via `model._gradient_checkpointing` attr. Prints block count + expected savings. (2) `build_optimizer` now reads `cfg_opt["use_8bit_optim"]` — when True, imports `bitsandbytes` and returns `bnb.optim.AdamW8bit` (same param groups, betas, eps as before); else returns `torch.optim.AdamW`. Import is inside the branch so m09a/m09b don't pay import cost. Added two yaml keys to `configs/train/base_optimization.yaml` under `optimization:` with safe defaults OFF (preserves m09a/m09b behavior): `use_8bit_optim: false`, `gradient_checkpointing: false`. Overridden to `true` in `configs/train/ch11_surgery.yaml`. m09c imports `enable_gradient_checkpointing` + calls it conditionally right after `build_model(cfg, device)` — only on student (teacher runs under torch.no_grad so no activation memory to save). m09c's optimizer rebuild per-stage at line 411 auto-picks up the 8-bit flag via the shared `build_optimizer`. Verified: `load_merged_config(vjepa2_1.yaml, ch11_surgery.yaml)` returns `use_8bit_optim=True, gradient_checkpointing=True`; bitsandbytes 0.49.2 imports cleanly; lint clean across utils/training.py + m09c_surgery.py. Expected Stage 3 memory on 24 GB: AdamW8bit optimizer state = 4.1 GB (was 16.5 GB), activations with checkpointing ~1.5-2 GB (was 5-6 GB), student+teacher fp16 = 7.5 GB, predictor+grads = 2.9 GB → total ~16 GB with ~8 GB headroom for fragmentation. Stages 1+2 proportionally better. Throughput hit: ~25% (checkpointing) + <1% (8-bit) = ~25%. SANITY's purpose per CLAUDE.md is "code correctness", so throughput hit is acceptable; FULL hardware (96 GB) will still benefit from 8-bit (marginally) but can disable checkpointing via yaml override if needed | `src/utils/training.py`, `src/m09c1_surgery_encoder.py`, `configs/train/base_optimization.yaml`, `configs/train/ch11_surgery.yaml` |
| 55 | After #54 fix, v5 SANITY exposed two critical correctness bugs that v4's UnboundLocalError had been masking. Stage 1: `OOM #1! sub-batch 4 → 2; OOM at step 0: macro discarded, sizer shrunk to 2, retrying next batch` → loop ended (1 step total in SANITY) → `Stage stage1_layout complete: 1 steps, loss=0.0000`. Same pattern stages 2 + 3 (sub-batch 2→1, then 1→giving up). Final: `SURGERY COMPLETE: 0 steps across 3 stages` exited with success and exported `student_encoder.pt` IDENTICAL to the frozen input V-JEPA weights. Plots show empty legends ("No artists with labels found"). The script PASSED CI but produced GARBAGE — exactly the silent-failure mode CLAUDE.md "Silent failures = garbage metrics" forbids | TWO independent bugs compound: **(A) Within-step retry missing**: helper raises on OOM with sizer.on_oom() shrinking inside; caller catches and `continue`s to next step. With `stage_steps=1` (SANITY) there is no next step → 0 successful steps. The intended adaptive-shrink semantics (try 4, fail, try 2, fail, try 1, ...) NEVER fire because each shrink moves to a non-existent next iteration. Pre-existing in m09b too (line 738) — masked there by `total_steps >> 1` so the next iteration's smaller sub-batch eventually succeeded. **(B) No 0-step fail-hard**: train_surgery completes its outer stage loop and falls through to `export_student_for_eval` regardless of whether any step actually ran. Combined with (A), produces a "successful" run that wrote zero training updates. **Underlying memory pressure** (Stage 3 OOM at sub-batch=1): 36/48 trainable blocks → 1.38B trainable params → AdamW state alone ≈ 16.5 GB (m1+m2+master fp32 at 12 bytes/param), plus student fp16 (3.7 GB) + teacher fp16 (3.7 GB) + predictor + grads + activations → ≥27 GB needed on a 24 GB Blackwell. Fundamental budget overflow regardless of micro-batch | **Three-part fix** (correctness only — memory mitigation deferred to user decision): **(1) Within-step retry loop** in m09c lines 459-489: replaced `try/except → continue` with `while not step_succeeded:` loop. On OOM: if `train_sizer.size <= train_sizer.min_size`, raise RuntimeError with mitigation hints (gradient checkpointing, bitsandbytes AdamW8bit, FULL hardware); else print "sub-batch shrunk → retrying SAME macro" and loop. Same pattern applied to m09b lines 730-760. **(2) 0-step fail-hard** in m09c line 549 after the `try/finally` block: `if global_step == 0: raise RuntimeError("SURGERY FAILED: 0 successful training steps... refusing to write misleading checkpoint")`. m09b not extended with post-loop check because its inside-loop fail-hard already covers the case (loop body always assigns `step_succeeded = True` before recording, so reaching the loop's end implies progress). **(3) Mitigation hints in the error message**: cites errors_N_fixes.md #55 + lists 3 gold-standard memory fixes. SANITY's purpose per CLAUDE.md is "code correctness", so failing loud at sub-batch=1 OOM IS the correct behavior — surgery on V-JEPA ViT-G with 36-block fine-tuning fundamentally requires either (a) gradient checkpointing (~25% throughput hit, well-tested PyTorch primitive) or (b) bitsandbytes AdamW8bit (4× optimizer-state reduction, HF/FAIR standard) or (c) FULL hardware (96 GB Blackwell). User to decide which path. Verified: lint clean (py_compile + ruff F821/F841/F811) on both files | `src/m09c1_surgery_encoder.py`, `src/m09b_explora.py` |
| 54 | After #53 fix, v4 SANITY made deepest progress yet — student 1.84B params loaded, teacher cloned, predictor 300/300 keys, factor index (D_L=20, D_A=12, D_I=9), Stage 1 setup printed (`AdaptiveBatchSizer (surgery, grad-accum): start=4, max=32, target VRAM=85%`), trainable prefix 12/48 blocks. Then OOM-recovery cycle worked correctly: `AdaptiveBatch: OOM #1! sub-batch 4 → 2; OOM at step 0: macro discarded, sizer shrunk to 2, retrying next batch`. But because SANITY mode runs only `Steps: 1` per stage and that single step hit the early-`continue` branch, the inner step-loop exited with no successful iteration → `train_surgery:507 print(... loss={jepa_val:.4f})` blew up: `UnboundLocalError: cannot access local variable 'jepa_val' where it is not associated with a value` | Per-stage end-of-loop summary print at line 507 references `jepa_val` (the last successful step's loss). When zero steps complete (e.g., SANITY with 1 step that OOMs and continues, or any stage where every step OOMs), the loop body never assigns `jepa_val` → undefined local at the print. m09b already has the canonical fix at lines 683-688: pre-init `jepa_val=masked_val=context_val=lr_val=gn_val=0.0` BEFORE the for-loop. m09c was missed during the #49 split because vanilla pretrain (m09a) and m09b have a single training-loop scope where one pre-init covers everything; m09c has 3 nested stages, each needing its own pre-init so each stage's summary reflects THAT stage's last value (not the prior stage's leak) | Inserted 5-line pre-init block in `m09c_surgery.py` between `pbar = make_pbar(...)` and `for local_step in range(stage_steps):` (after line 423). Each stage iteration resets `jepa_val=0.0, masked_val=0.0, context_val=0.0, lr_val=0.0, gn_val=0.0` so an OOM-only stage prints `loss=0.0000` instead of crashing. Mirrors m09b lines 683-688. Comment cites the m09b reference and explains the per-stage scope choice. Verified: lint clean (py_compile + ruff F821/F841/F811); no other unbound-var risk in m09c (grep for variables referenced after the for-loop returns only this set). NOTE: SANITY mode's `Steps: 1` means the print will always show `loss=0.0000` until POC mode (where `total_steps >> 1` ensures at least some successful step). The fix is correctness, not training quality — POC will validate actual loss trajectory | `src/m09c1_surgery_encoder.py` |
| 53 | After #52 fix, v3 SANITY successfully built student/teacher/predictor + factor sampler + Stage 1 unfreeze (12/48 blocks, 25%) → crashed at first forward pass: `OOM in teacher GELU MLP, tried to allocate 2.25 GiB, only 1.87 GiB free` (24 GB Blackwell). Per CLAUDE.md, lazy fix would be lowering `batch_size`; gold-standard fix is `AdaptiveBatchSizer + _train_step_grad_accum` (already wired in m09a/m09b per #48). m09c still had the inline forward+backward block from the monolithic m09c agent split (#49 phase 2.3) — sizer + helper imports were stubbed `# noqa: F401 — wired via utils.training` but never actually wired | m09c_surgery's 3-stage progressive-unfreeze loop predates the #48 grad-accum integration. The split-time agent kept the inline forward block to preserve surgery semantics, deferring sizer wiring as "future". Without it: 24 GB GPU at training_batch_size=32 produces ~22 GB peak activations (teacher+student+predictor+grads), no headroom for any spike → OOM at first GELU. PYTORCH_CUDA_ALLOC_CONF was also unset across all 3 m09 modules (m05 had it; m09a only had a comment) — fragmented blocks compound the issue | **Three-part fix** applied to all 3 m09 modules consistently: (1) `_train_step_grad_accum` wired into m09c's surgery loop — replaces inline forward+backward block at lines 444-466, calls helper with `init_params=None, drift_cfg=None` (surgery has no drift), captures macro-batch mean losses (jepa_val/masked_val/context_val) for diagnostics, OOM → optimizer.zero_grad() + continue (preserves per-stage state). (2) `AdaptiveBatchSizer(initial=yaml.training_initial_bs=4, max=batch_size, memory_cap=yaml.gpu_memory_target=0.85)` initialized after total_steps computation in train_surgery — same pattern as m09a/m09b. (3) `os.environ.setdefault("PYTORCH_CUDA_ALLOC_CONF", "expandable_segments:True")` added at top of all 3 m09 main()s (was set in m05 but missing in m09a/b/c). Verified: lint clean across all 3 files; uniform wiring confirmed via `grep -c` (each module has 1 sizer init, 1 helper call, 1 OOM try/except, ≥1 env-var setdefault) | `src/m09a1_pretrain_encoder.py`, `src/m09b_explora.py`, `src/m09c1_surgery_encoder.py` |
| 52 | After #51 fix, m09c SANITY got further (student 1.84B params loaded, teacher+predictor built, factor index indexed 20 clips: D_L=20, D_A=12, D_I=9) then crashed at `train_surgery:351 TypeError: 'int' object is not subscriptable`. Line read `cfg["optimization"]["max_epochs"][mode_key]` — redundant because `merge_config_with_args:88` already flattened the per-mode dict (`{sanity: 1, poc: 1, full: 3}`) into an int | Pre-existing bug from the monolith — `train_surgery()` in the original m09_pretrain.py had the same double-subscript. Never surfaced because (a) nobody ran it end-to-end recently, or (b) an earlier config path kept `max_epochs` as the dict. m09a and m09b do it correctly (line 401/407: `max_epochs = cfg["optimization"]["max_epochs"]` — single subscript). m09c's copy-paste of train_surgery kept the stale pattern | Single-line fix at `m09c_surgery.py:352`: `cfg["optimization"]["max_epochs"][mode_key]` → `cfg["optimization"]["max_epochs"]`. Added inline comment explaining the flatten already happened at line 88. Verified: lint clean; no other `mode_key` re-subscripts in m09c | `src/m09c1_surgery_encoder.py` |
| 51 | After #50 fix, second m09c SANITY run crashed at `build_model:125 data_cfg["patch_size"] KeyError`. Same error would hit m09a / m09b eventually. 18 occurrences across 4 files (m09a/b/c + utils/training.py) all referencing `crop_size`, `patch_size`, `tubelet_size` under `cfg["data"]` — but the YAML schema puts them under `cfg["model"]` (`configs/model/vjepa2_*.yaml:40-42`). Only `num_frames` correctly lives in `cfg["data"]` (from `configs/train/base_optimization.yaml:16`) | Pre-existing bug in the original `m09_pretrain.py` — never hit because `train()` and `train_surgery()` paths in the monolith either (a) didn't reach `build_model` in recent sessions under the current config merge, or (b) the bug was masked by a prior config merge that copied model keys into data. Exposed now because the split agents literally copied line-by-line and CLAUDE.md's fail-hard rule means there's no `.get(default)` safety net (correctly so — this is exactly the bug fail-hard catches). Our B6 preflight guard wouldn't catch this since `cfg["data"]["patch_size"]` is a plain subscript, not a `.get()` call | One-shot sed across 4 files to rename `data_cfg["crop_size"]` → `model_cfg["crop_size"]` (and same for `patch_size`, `tubelet_size`). Left `data_cfg["num_frames"]` unchanged — that key IS correctly in `cfg["data"]`. Added `model_cfg = cfg["model"]` to `build_mask_generators` in utils/training.py (didn't have it before). Verified via: `load_merged_config(vjepa2_1.yaml, ch11_surgery.yaml)` → `cfg["model"]["crop_size"]=384, patch_size=16, tubelet_size=2, cfg["data"]["num_frames"]=16` all resolve. Lint clean across all 4 files | `src/m09a1_pretrain_encoder.py`, `src/m09b_explora.py`, `src/m09c1_surgery_encoder.py`, `src/utils/training.py` |
| 50 | First run of `src/m09c1_surgery_encoder.py --SANITY` (post-split #49) crashed at `build_model` → `utils/vjepa2_imports.py:151 get_vit_predictor()` with `KeyError: 'src.models.predictor'`. The 2.0 base predictor lookup hit a missing key in `sys.modules` | Pre-existing latent bug in `_ensure_loaded_2_1()` exposed by the split. The 2.1 loader **pops ALL `src.*` modules** from `sys.modules` (including `src.models.predictor`, `src.masks.multiseq_multiblock3d`, etc. that `_ensure_loaded_base()` just imported) to avoid namespace collision during its own re-import path. Its `finally` block only restored `src` and `src.utils` — the 2.0 submodules (`src.models.predictor`, `src.models.vision_transformer`, `src.masks.utils`, `src.masks.multiseq_multiblock3d`) were left popped. Subsequent calls to `get_vit_predictor()` / `get_mask_generator()` read `sys.modules[...]` directly and short-circuit `_ensure_loaded_base()` via the `_loaded_base=True` flag → KeyError. Monolithic m09_pretrain.py was only affected in tests that both (a) triggered `_ensure_loaded_2_1` (via ViT-G 2.1) AND (b) subsequently called `get_vit_predictor()` (2.0 base) — m09c's `build_model` does exactly this sequence | In `_ensure_loaded_2_1()`'s `finally` block, restore **all** `saved_modules` entries, not just `src` and `src.utils`. Exception: skip modules that were freshly re-imported during 2.1 load (`src.utils.tensors`, `src.masks.utils`) — keeping the re-imported versions preserves any 2.1-path state if it differs. Check `if key not in sys.modules` before assignment to avoid clobbering 2.1-refreshed modules. Verified by reproducing m09c's call order: `get_vit_by_arch("vit_gigantic_xformers")` → `get_vit_predictor_2_1()` → `get_vit_predictor()` → `get_mask_generator()` → all 5 symbols load cleanly | `src/utils/vjepa2_imports.py` |
| 49 | `src/m09_pretrain.py` grew to **2164 lines** housing 3 entangled training techniques (vanilla Ch10, ExPLoRA, Ch11 surgery) via `--explora`/`--surgery` argparse flags. Each technique is unproven (Step D ExPLoRA + Step E Surgery both pending first run) and expected to need many failure-driven iterations. Risk: iteration on one technique silently regresses the other two — the exact failure mode that bit Ch10 catastrophic forgetting in `iter/iter7_training_full/runbook.md` where intermixed logging masked divergence | Monolith violates CLAUDE.md "Modules MUST NOT repeat numbers" in spirit — one file for 3 techniques means every edit touches all 3. User-requested 3-way physical split with full isolation ("modification in m09c_surgery.py should not break well-matured m09b_explora.py"). Future training techniques (BitFit / IA³ / adapters per `iter/iter8/FactorJEPA-Alternatives*.pdf`) become NEW files, never edits to validated ones | **4-phase split executed 2026-04-15 via 3 parallel agents**. Phase 1: extracted 20 shared helpers from m09 → new `src/utils/training.py` (715 lines). Contract: zero `args.explora`/`args.surgery`/`cfg["technique"]` branches in utils/training (verified by grep). Phase 2: created `src/m09a1_pretrain_encoder.py` (1174 lines, vanilla + drift + lambda ablation), `src/m09b_explora.py` (1030 lines, LoRA hardcoded on, no drift), `src/m09c1_surgery_encoder.py` (599 lines, 3-stage progressive unfreeze + factor datasets). Each has its own FULL training loop (user-chosen full isolation). Deleted original `src/m09_pretrain.py`. Phase 3: updated 3 scripts (`train_pretrain.sh` → m09a, `train_explora.sh` → m09b dropping `--explora`, `train_surgery.sh` → m09c dropping `--surgery`), 1 utils (`profile_vram.py` import), 3 configs (docstrings), `scripts/run_embed.sh:90` glob now covers both new (m09a/b/c) and legacy (m09_pretrain) output paths. Created `scripts/migrate_m09_outputs.sh` (idempotent `mv` migration for pre-existing checkpoints — none existed at migration time). Phase 4: this log entry + docs. Danger zones mitigated: (1) `main._nan_strikes` global → local `nan_strikes` in each m09[abc] `train()`; (2) `init_params` per-technique (m09a: all params, m09b: filter LoRA, m09c: not built); (3) `drift_cfg` only in m09a; (4) `explora_enabled` threaded explicitly to `export_student_for_eval` (m09b=True, others=False); (5) `producer_thread` not in m09c (uses FactorSampler); (6) `run_validation` not in m09c; (7) per-technique logging schemas (m09a has `loss_drift`, others don't); (8) ablation sweep only in m09a. Lint clean across all 5 files (`py_compile + ruff F821/F841/F811`). Smoke imports pass for all 3 new modules. Backup at `/tmp/m09_pretrain_backup.py` | `src/m09_pretrain.py` (deleted), `src/m09a1_pretrain_encoder.py` (new), `src/m09b_explora.py` (new), `src/m09c1_surgery_encoder.py` (new), `src/utils/training.py` (new), `src/utils/profile_vram.py`, `scripts/{train_pretrain,train_explora,train_surgery,run_embed}.sh`, `configs/train/{ch10_pretrain,explora,ch11_surgery}.yaml`, `scripts/migrate_m09_outputs.sh` (new) |
| 48 | Follow-up to #47: m09 had per-epoch `gc + empty_cache` only (no mid-step adaptive sizing — flagged "research-coupled, deferred"). User pushed back: `training_batch_size` must adapt to 85% GPU utilization too, same as inference. Naive solution (resize producer batch) violates research integrity — changes effective BS → changes Adam momentum scale + LR scaling + weight-decay schedule → invalidates ch10 catastrophic-forgetting comparison (`iter/iter7_training_full/runbook.md`). Real solution is **gradient accumulation with adaptive micro-batch size** | Effective batch size MUST stay = `cfg.optimization.batch_size` (yaml `training_batch_size`) so optimizer dynamics are bit-identical to a static-BS run. The micro batch (per forward+backward) can be smaller and adaptive. Each micro's loss is scaled by `(micro_size / macro_size)` so `sum_i(scaled_micro_grad_i) == single_full_batch_grad` mathematically. This is exactly what HF Trainer + Lightning + DeepSpeed do | (a) Added `_train_step_grad_accum()` helper after `compute_drift_loss`. Sub-batches `batch_clips + all_masks_enc + all_masks_pred` along dim 0 by `sizer.size`, runs forward+backward per micro inside autocast, scales each micro's loss by `weight = micro_bs/macro_bs` before `scaler.scale(...).backward()` (gradients accumulate). Drift loss (no batch dim) added once after the micro loop with full weight. Returns `(jepa_val, masked_val, context_val, drift_val)` — the macro-batch mean losses (weighted sum of micros) — preserving the per-step diagnostics that ch10 needed for catastrophic-forgetting detection. (b) `train()`: initialize `train_sizer = AdaptiveBatchSizer(initial=yaml.training_initial_bs, max=batch_size, memory_cap=yaml.gpu_memory_target)` once before the training loop. Replace inline forward+loss+backward block with `_train_step_grad_accum(...)` call. On OOM: `optimizer.zero_grad()` to discard partial grads, sizer already shrank via `on_oom`, `continue` to next macro batch. Single `scaler.unscale + grad_norm + scaler.step + scaler.update + zero_grad + scheduler.step` per macro — preserves effective BS. (c) `loss_exp` passed explicitly to helper (not `cfg.get(.,.)` which violates CLAUDE.md no-yaml-defaults). (d) NaN guard, CSV log, wandb log, pbar postfix all unchanged — `jepa_val/masked_val/context_val/drift_val/total_val` flow through. **train_surgery() not yet refactored** — same pattern applies but no drift loss; deferred to follow-up if Step E OOMs | `src/m09_pretrain.py` |
| 47 | Repo-wide INFRA gap (audit triggered by #46): `AdaptiveBatchSizer` was implemented in `utils/gpu_batch.py` but ONLY wired into m04 VLM and m04d RAFT. Every other GPU script (m05/m05b/m05c/m09/m10) had hardcoded BS picked at startup → no adaptive growth toward 85% VRAM target → either OOM (24GB) or massive idle (96GB). Static `memory_cap=0.85` in m04d, default 0.85 in m04 — neither read from yaml. Violated CLAUDE.md "GPU util ≥85% TOP PRIORITY" universally | Sizer is INFRA, must be in every GPU batch loop. Three universal yaml keys (`gpu_memory_target` + per-module `*_initial_bs`) in `configs/pipeline.yaml` give one source of truth | **(a) `configs/pipeline.yaml`**: added universal `gpu_memory_target: 0.85` (used by ALL sizers across m04/m05/m05b/m05c/m09/...) + per-module initial sizes: `inference_vjepa_initial_bs: 8`, `inference_dinov2_initial_bs: 16`, `inference_clip_initial_bs: 16`, `training_initial_bs: 4`. **(b) m04_vlm_tag**: replaced default `memory_cap` with explicit yaml read. **(c) m04d_motion**: replaced hardcoded `memory_cap=0.85` with yaml read. **(d) m05b_baselines**: integrated `AdaptiveBatchSizer` in DINOv2 + CLIP runners — sub-batch the producer batch via sizer, OOM → `cuda_cleanup()` + `sizer.on_oom()` → retry, success → `sizer.after_batch_success()` → may grow toward 85%. **(e) m05c_true_overlap**: added sizer for paired-forward overlap (initial = vjepa_initial // 2 since pixels_a + pixels_b = 2× memory). **(f) m09_pretrain**: per-epoch `gc.collect()` + `empty_cache()` + `ipc_collect()`. Full sizer + gradient-accumulation refactor for training-step deferred (changes optimizer dynamics — research decision, log as backlog). **(g) m10_sam_segment**: per-clip SAM3 sessions don't have a batch axis to sub-batch, but added `empty_cache()` every 10 clips for fragmentation hygiene. **(h) Lint**: all 7 files clean under py_compile + ruff F821/F841/F811 | `configs/pipeline.yaml`, `src/m04_vlm_tag.py`, `src/m04d_motion_features.py`, `src/m05_vjepa_embed.py`, `src/m05b_baselines.py`, `src/m05c_true_overlap.py`, `src/m09_pretrain.py`, `src/m10_sam_segment.py` |
| 46 | m05 OOM on 24GB Blackwell at BS=100 (retry BS=50 also OOMed) — orchestrator's `inference_adapted_bs: 44` cap was skipped when `args.model=None` (V-JEPA 2.1 native frozen) because `is_adapted_pre=False` via `_resolve_model(None)`. BS stayed at 176 from the 96GB profiler. Even at cap=44, 24GB can't fit static BS=44 at bf16 × 2B × 64 frames × flash-attn-2. User also flagged CLAUDE.md: "GPU util ≥85% TOP PRIORITY" — a static BS=8 floor would waste 60% of 24GB VRAM and 85% of 96GB VRAM | Single hardcoded BS can't be right for both 24GB and 96GB simultaneously. Static cap protects CPU queue + recompile thrash but wastes GPU. `compute_batch_sizes()` is a static formula (linear scale from 40GB baseline), not adaptive. `AdaptiveBatchSizer` in `utils/gpu_batch.py` IS adaptive (grow until X% VRAM, halve on OOM, cooldown) and is already used by m04 VLM — just never wired into m05 | **(a) `configs/pipeline.yaml`**: added `inference_vjepa_initial_bs: 8` (safe start for native V-JEPA path) + `gpu_memory_target: 0.85` (universal VRAM ceiling for all AdaptiveBatchSizer users — m04/m05/m09/…). Kept `inference_adapted_bs: 44` as the MAX cap (guards CPU queue ≤40GB host RAM per cgroup limit + torch.compile recompile thrash). **(b) `src/m05_vjepa_embed.py`**: extended the adapted-BS cap to also apply when V-JEPA 2.1 native-frozen is loaded (args.model=None + hf_model_id=null → same native fwd path as adapted student). Producer now fills batches at the MAX cap; consumer sub-batches via `AdaptiveBatchSizer(initial_size=yaml, max_size=yaml, memory_cap=yaml)` — grows geometrically until 85% VRAM, halves on OOM, raises if min hit. All 3 sizer params from yaml, zero hardcoded Python defaults. Also: set `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` at m05 main() entry so OOM retry actually reclaims memory (v6 50-half failed because fragmented blocks stayed stranded) | `configs/pipeline.yaml`, `src/m05_vjepa_embed.py` |
| 45 | Follow-up to #44 after the RoPE+bf16 fix: m05 crashed at `conv3d` in `patch_embed` (`vision_transformer.py:285`, `patch_embed.py:71`) with `Input type (c10::Half) and bias type (c10::BFloat16) should be the same`. Model was now bf16 (correct) but input pixel tensors were still being cast to fp16 via 3 hardcoded `dtype=torch.float16` sites in m05 | Three hardcoded fp16 assumptions that only held while the model was also fp16. When #44 switched V-JEPA 2.1 to bf16, fp16 inputs + bf16 weights hit `F.conv3d`'s dtype-check (same rigor as the RoPE Q/K/V check). Classic "second-mismatch after first-mismatch fix" pattern | Replaced all 3 hardcoded fp16 sites with runtime dtype detection: `_m = getattr(model, '_orig_mod', model); dt = next(_m.parameters()).dtype` (the `_orig_mod` unwraps torch.compile-wrapped models). Applied to: (a) input cast in `get_batch_embeddings` line 226; (b) `torch.amp.autocast` context for HF AutoModel path line 236; (c) warmup tensor line 597. Remaining `dtype=torch.X` literals at lines 530/573/583 are intentional — they're the single source-of-truth at model-load time (adapted fp16, native bf16, HF fp16); all downstream code now reads from the loaded model. Pattern generalizes to the planned V-JEPA 2.0 / 2.1 dual-path refactor | `src/m05_vjepa_embed.py` |
| 44 | m05 V-JEPA 2.1 frozen load crashed under `torch.compile` with `RuntimeError when making fake tensor call ... F.scaled_dot_product_attention(q.dtype=float, k.dtype=float, v.dtype=c10::Half)`. Happened AFTER #42 fix (correct frozen-native branch taken, 588/588 params loaded). Crash at `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py:287` during `torch.compile` inductor tracing | V-JEPA 2.1 RoPE (`rotate_queries_or_keys` in `rope.py`) computes sin/cos/einsum in fp32 for numerical stability → Q, K come out fp32 even though model was cast to fp16. V skips RoPE → stays fp16. PyTorch 2.12 nightly's FakeTensor strict check rejects Q/K/V dtype mismatch (stable 2.5 was lenient). **Initial reflex** was to skip torch.compile → rejected (2B model eager = too slow). Gold-standard fix (websearched across HF Llama, naver-ai/rope-vit, HF PR #36065 Qwen2.5-VL, Dao-AILab/flash-attention #1341, #1987) is to cast Q, K back to V.dtype right after RoPE — rotation math stays fp32, only final result is downcast, lossless vs. keeping Q/K fp32 | **(a)** Patch `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py` — after the Q/K concat following RoPE (line 284, pre-SDPA): `q = q.to(v.dtype); k = k.to(v.dtype)`. Matches HF Llama's `apply_rotary_pos_emb ... .to(q.dtype)` and naver-ai/rope-vit's `.type_as(q)`. **(b)** Switch m05 V-JEPA 2.1 native load dtype from fp16 → bf16 (same FA2 throughput on SM120, wider dynamic range avoids NaN on long video token sequences). Keeps FA2 via SDPA dispatch, keeps torch.compile + inductor memory savings (89 → 30 GB at BS=176). Zero accuracy impact | `src/m05_vjepa_embed.py`, `deps/vjepa2/app/vjepa_2_1/models/utils/modules.py` |
| 43 | Repo-wide `torch_dtype=` audit (follow-up to #37): 6 additional `from_pretrained(..., torch_dtype=...)` call sites found across the GPU scripts — **would silently use default dtype under transformers 5.5.4** (deprecation warning, not a hard crash; but fp32 load = OOM on 24GB for a 2B model). Modules affected: m04_vlm_tag (VideoLLaMA3 bf16 + LLaVA-NeXT fp16), m05b_baselines (V-JEPA 2.0 + CLIP + DINOv2, all fp16), m05c_true_overlap (one fp16 loader) | Same root cause as #37 — this is the same deprecation; I fixed only the DINO+Sam3 sites during the 4.57→5.5.4 migration and missed the VLM/baseline loaders. Found via `grep -rn "torch_dtype" src/m*.py` after preflight B16 flagged m05_vjepa_embed:562 | One-line rename per call site: `torch_dtype=torch.X` → `dtype=torch.X  # #37`. All 6 sites edited. Verified 0 actual kwarg usages remain via `grep -nE "\\btorch_dtype\\s*=" src/m*.py`. py_compile + ruff clean on all 6 files. m10_sam_segment.py already fixed during the v2_HF integration, m11_factor_datasets.py has no model loads, so all surfaces now consistent | `src/m04_vlm_tag.py`, `src/m05b_baselines.py`, `src/m05c_true_overlap.py` |
| 42 | **Two-stage bug in `m05_vjepa_embed.py` loading V-JEPA 2.1 frozen checkpoint.** Stage 1: `Path(args.model)` crashed with `TypeError ... not 'NoneType'` because `VJEPA_MODEL_ID` is None when the default YAML has `hf_model_id: null` (V-JEPA 2.1 has no HF release). Stage 2 (after my initial `args.model = VJEPA_CHECKPOINT_PATH` patch): worker loaded the native `.pt` but routed to the **adapted-student branch** (line 488) which expects `ckpt['student_state_dict']`, not the **native-frozen branch** (line 523) which correctly tries `ckpt.get('target_encoder') ?? ckpt.get('encoder')`. Result: `Adapted encoder: loaded 0/588 params ... FATAL: key mismatch!` | Branch selector is `is_adapted = model_path.suffix == ".pt" and model_path.exists()` — but the 2.1 frozen checkpoint IS a .pt file, so setting `args.model` to it mis-triggered `is_adapted=True`. The correct invariant: `is_adapted=True` means "user passed --model X.pt from m09 surgery training"; `is_adapted=False` with `hf_model_id=null` means "load frozen 2.1 native from YAML checkpoint_path" | Introduced `_resolve_model(user_model)` helper that returns `(Path or None, is_adapted)`. When `user_model is None` → returns `(None, False)`, which routes to the native-frozen branch that already unpacks the `target_encoder`/`encoder` keys correctly. Replaced all 4 `Path(args.model)` / `is_adapted = ...` duplicates (orchestrator + worker) with one-line calls to the helper. Dropped the earlier wrong `args.model = VJEPA_CHECKPOINT_PATH` patch + its import | `src/m05_vjepa_embed.py` |
| 41 | P-3a probe crashed after main P-5a pipeline finished 100/100 clips: `TypeError: Sam3VideoProcessor.add_text_prompt() got an unexpected keyword argument 'prompts'`. The reference I used in `plan_code_dev.md` (from Meta's model card) showed `prompts=["person","bed","lamp"]` but the real API in transformers 5.5.4 is `text: str \| list[str]`. Second issue once fixed: my code did `post["pred_masks"]` but `postprocess_outputs()` returns `{object_ids, scores, boxes, masks, prompt_to_obj_ids}` (key is `masks`, not `pred_masks`; and it's a `(N, H, W)` tensor, not a list) | Meta's model card documentation on HF hub is slightly ahead of / divergent from the released 5.5.4 API. Pattern: for any kwarg/key taken from model-card examples, verify against the actual installed source before shipping. Two related diff in Sam3Video vs Sam3TrackerVideo — Sam3Video's postprocess returns `masks`, Sam3TrackerVideo's output has `pred_masks` directly on the Output dataclass. Different subsystems, different attribute naming, both valid | (a) `probe_p3a_text_only()`: `prompts=taxonomy` → `text=taxonomy`. (b) `post["pred_masks"]` → `post["masks"]`; loop index over `masks_t.shape[0]` and `.detach().cpu()` per slice; guard `masks_t.numel() == 0` for empty frames. Confirmed via `inspect.getsource(Sam3VideoProcessor.postprocess_outputs)` + `dataclasses.fields(Sam3VideoSegmentationOutput)`. Add preflight B21 to grep legacy kwarg `add_text_prompt\(.*prompts=` and legacy key `post\[.pred_masks.\]` on Sam3Video code paths | `src/m10_sam_segment_v2_HF.py` |
| 40 | **Silent bug** in v2_HF caught by preflight: code referenced `output.iou_scores` (SAM2 attr name) on HF `Sam3TrackerVideoSegmentationOutput`, which actually exposes `(object_ids, pred_masks, object_score_logits, frame_idx)`. Used `getattr(output, "iou_scores", None)` with fallback to `np.ones(n_objs)` → every mask confidence set to 1.0 → `min_confidence=0.3` filter never rejects anything → no crash, but D_A quality silently degrades | SAM2 output uses `iou_scores` (IoU prediction). SAM3 Tracker renamed to `object_score_logits` (raw logits, needs sigmoid) — confirmed via `dataclasses.fields(Sam3TrackerVideoSegmentationOutput)`. Matches the general SAM3 design: scores are logits, callers apply sigmoid as needed. This bug would NOT have been caught by any runtime test (process runs to completion) — only by static attr inspection vs the dataclass fields | In `segment_clip()` of `m10_sam_segment_v2_HF.py`: `probs = torch.sigmoid(output.object_score_logits.detach().float()).squeeze().cpu().numpy()`. Remove the `getattr` fallback (fail-hard if attr missing). Same line for ndim==0 scalar handling. Added preflight check **B20** to grep for legacy SAM2/raw-sam3 output attrs (`iou_scores`, `mask_logits`, `out_obj_ids`, `out_binary_masks`, `out_probs`) to catch this on any future Sam3TrackerVideo code | `src/m10_sam_segment_v2_HF.py` |
| 39 | v2_HF second-category crash: `AttributeError: 'Sam3TrackerVideoProcessor' object has no attribute 'reset_inference_session'` at `segment_clip()` after first anchor×category finished. First anchor ran cleanly through DINO → add_inputs → propagate; crash fired when the code tried to clear prompts for the next category | HF API puts reset/clear methods on the **session object** (`Sam3TrackerVideoInferenceSession`), not the processor. Confirmed via grep: `modeling_sam3_tracker_video.py` defines `session.clear_all()`, `session.remove_point_inputs()`, `session.remove_mask_inputs()`, `session.reset_tracking_data()`, `session.reset_inference_session()`. Two flavors matter: `reset_tracking_data()` clears obj_ids + prompts + tracklets while **keeping the encoded video cache** (cheap); `reset_inference_session()` also clears the cache (expensive, forces re-encoding every category) | In `segment_clip()` of `m10_sam_segment_v2_HF.py`: `sam_processor.reset_inference_session(session)` → `session.reset_tracking_data()`. Keeps per-clip video features cached across all 4 anchors × ~3 categories = ~12 resets per clip without re-encoding each time. Expensive `reset_inference_session()` or `clear_all()` would negate the Path B speedup | `src/m10_sam_segment_v2_HF.py` |
| 38 | v2_HF first-clip crash inside `Sam3TrackerVideoProcessor.add_inputs_to_inference_session` → `_validate_single_input`: `ValueError: Input boxes must be a nested list with 3 levels. The expected nesting format is [image level, box level, box coordinates]. Got 4 levels.` | Confused box API with point API. `Sam3TrackerVideoProcessor.__call__` validates `input_points` at `expected_depth=4` ([image, object, point, coords]) but `input_boxes` at `expected_depth=3` ([image, box, coords]). Points allow multi-point-per-object; boxes are flat: one box per obj_id, parallel with the `obj_ids=[...]` list. Our code `[[[b] for b in boxes_xyxy]]` wrapped each box in its own list (point-API style), producing depth 4 | Change `input_boxes=[[[b] for b in boxes_xyxy]]` → `input_boxes=[boxes_xyxy]` in `segment_clip()` of `m10_sam_segment_v2_HF.py`. `boxes_xyxy` is already `[[x1,y1,x2,y2], ...]` so wrapping in one outer list gives the required `[[[x1,y1,x2,y2], ...]]` = 3 levels. obj_ids remain parallel (`[0,1,2,...]`). Confirmed by `inspect.getsource(Sam3TrackerVideoProcessor.__call__)` line 71-77 showing `expected_depth=3, expected_coord_size=4` for boxes | `src/m10_sam_segment_v2_HF.py` |
| 37 | DINO dtype crashes under transformers 5.5.4 — **two symptoms, one root cause**. v1 (fp16 DINO + fp16 cast on inputs): `Input type (float) and bias type (c10::Half)` at Swin `conv_encoder.forward:387`. v2 (after casting pixel_values to fp16): `mat1 and mat2 must have the same dtype, but got Float and Half` at `text_enhancer_layer.self_attn.query` Linear:1154 | transformers 5.x removed **all** auto-casts in DINO's cross-modal encoder. Vision path we can cast (pixel_values in inputs dict). Text path we CAN'T: input_ids is int → embedding lookup produces fp32 activations → fp32 queries hit fp16 Linear weights → crash. Only full `torch.amp.autocast` would fix the text branch, which is too invasive (no wrap around every forward). v1 worked under transformers 4.57 because 4.x had internal casts on both branches | **Load DINO in fp32, not fp16.** DINO-base = 233 M params = ~1 GB fp32 vs ~500 MB fp16 → +500 MB VRAM, negligible next to SAM3's ~12 GB. 4 forwards/clip, forward latency diff is ≤5 %. Applied to both `load_grounding_dino` (v1 + v2_HF): drop `dtype=torch.float16` kwarg, let torch default to fp32. The defensive `inputs[k].to(model_dtype)` cast stays — no-op when model_dtype == fp32, future-proof if we later switch to autocast | `src/m10_sam_segment.py`, `src/m10_sam_segment_v2_HF.py` |
| 36 | Path B speedup for FULL 115K: raw `facebookresearch/sam3` pkg's `max_frame_num_to_track` is unfixable (#33/#35). HF `transformers.Sam3TrackerVideoModel` wraps the same SAM 3 weights but its `propagate_in_video_iterator(start_frame_idx=anchor, max_frame_num_to_track=segment_size-1)` correctly clips `processing_order` to valid frames. Unlocks ~10× m10 speedup (current v5 `~61 days` for 115K on 24GB → Path B `~6 days`). Blocker: Sam3 HF classes were added in `transformers>=5.0`; installed 4.57.6 had only Sam2 → direct import crashed with `ImportError: cannot import name 'Sam3TrackerVideoModel' from 'transformers'` | Our `requirements_gpu.txt` pinned `transformers>=4.57.0,<5.0` (older comment: "VideoLLaMA3 remote code breaks on 5.x"). VideoLLaMA3 is NOT m04's default backend (Qwen3-VL is, and our existing tags.json was already produced). Pinning up to 5.5.4 is a net win: unlocks `Sam3VideoModel` (P-3a text-only probe) + `Sam3TrackerVideoModel` (P-5a primary). Actively-used HF classes — Qwen3-VL (m04), AutoModelForZeroShotObjectDetection (m10 DINO), AutoProcessor (m04/m05) — have stable APIs across the 4.57→5.5 bump | **(a)** `requirements_gpu.txt`: `transformers>=4.57.0,<5.0` → `transformers==5.5.4` with a note that m04's VideoLLaMA3 backend may break (Qwen3-VL still works). **(b)** New `src/m10_sam_segment_v2_HF.py`: `load_sam3_hf()` builds `Sam3TrackerVideoModel` + `Sam3TrackerVideoProcessor` with bfloat16. `segment_clip()` uses HF API — one `init_video_session(video=frames_np)` per clip; per anchor × category `add_inputs_to_inference_session(frame_idx=anchor, obj_ids=..., input_boxes=[[[xyxy]...]])`; `propagate_in_video_iterator(start_frame_idx=anchor, max_frame_num_to_track=segment_size-1, reverse=False)`; `post_process_masks([output.pred_masks], original_sizes=[(H,W)], binarize=True)`; `reset_inference_session(session)` between categories to clear prompts without re-encoding video features. **(c)** `--probe-p3a N` CLI flag added: after P-5a finishes, run `Sam3VideoModel` (text-only PCS, Block 3a) on first N cached clips using our 17-cat taxonomy; saves masks to `masks_p3a/`. If P-3a mask quality ≥ P-5a on Indian scenes → drop DINO for FULL (saves ~2× additional compute). Awaiting `./setup_env_uv.sh --gpu --from-wheels` to install transformers 5.5.4, then POC dense100 run + A/B test | `requirements_gpu.txt`, `src/m10_sam_segment_v2_HF.py` |

## SAM3 `--no-deps` Undeclared Dependencies (pattern)

SAM3 is installed with `--no-deps` (error #5) to prevent it from downgrading numpy to <2.0 (which would break cuML). The cost: every runtime dep SAM3 uses must be declared explicitly in `requirements_gpu.txt`. Errors #6, #7, #18, #19 are all instances of this pattern.

**Pre-scan result** (grep of `venv_walkindia/lib/python3.12/site-packages/sam3/` import statements):

| Dep | On inference path (`build_sam3_predictor`)? | Status |
|---|---|---|
| `pycocotools` | Yes (via `train/data/coco_json_loaders.py` imported at load time) | Fix #6 |
| `einops` | Yes (`sam/rope.py`) | Fix #7 |
| `iopath` | Yes (`model_builder.py`) | Fix #18 |
| `ftfy` | Yes (`model/tokenizer_ve.py`) | Fix #19 |
| `regex` | Yes — but already transitive via transformers | Covered |
| `hydra`, `omegaconf`, `torchmetrics`, `submitit`, `openai` | No — only in `sam3/train/` and `sam3/agent/` (training/agent code paths, not inference) | Safe to omit |
| `skimage`, `sklearn` | Yes (`visualization_utils.py`) — but already in base `requirements.txt` | Covered |

If another SAM3 dep error surfaces, same fix: add to `requirements_gpu.txt`, re-run `./setup_env_uv.sh --gpu --from-wheels`. Do NOT use `uv pip install` directly (CLAUDE.md rule: reproducibility).

---

## Post-2026-04-17 additions (2026-04-19 session)

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 62 | 2026-04-19: Step A v1-1K `python -u src/m10_sam_segment.py --POC --subset data/val_1k.json --local-data data/val_1k_local` completed in 337 s with `Clip limit: 100` despite val_1k.json containing 1000 clip_keys. Runbook expectation was 102 min wall / 40-60K agents / 60-90K interactions; got 100-clip-scale output instead — the retired Phase 2a 100-dense POC results, silently produced under a 1K flag. | `src/m10_sam_segment.py:755-756` elif branch: `elif args.POC: clip_limit = train_cfg["poc_simplified"]["n_clips"]`. That yaml key defaulted to `100` (calibrated for the retired 100-dense discovery tier 2026-04-17). Explicit `--subset data/val_1k.json` was IGNORED because the POC branch never reached `get_total_clips()`. Same silent-fallback class as #60/#61 but on clip count instead of epoch/warmup. | **Removed the elif entirely** — m10 POC now flows through the same `get_total_clips(local_data=args.local_data, subset_file=args.subset)` path as FULL, with a clean FATAL if neither yields a count. Also **deleted the whole `poc_simplified:` yaml block** from `configs/train/ch11_surgery.yaml` so no other reader can accidentally re-introduce the cap. Mirrored the fix into `m10_sam_segment_legacy.py` for consistency. Verified on 2026-04-19 second run: `Clip limit: 1000` ✓ at log line 20. Preflight guard NOT added because the whole class is gone (no yaml key to check against a subset size). | `src/m10_sam_segment.py`, `src/m10_sam_segment_legacy.py`, `configs/train/ch11_surgery.yaml` |
| 63 | (Not an error — landed feature.) Surgery D.2 POC ran to "SURGERY COMPLETE" with only ONE trajectory data point (final Stage 3 loss) — impossible to distinguish "training is going in the right direction" from "training accidentally produced a matching endpoint" without mid-training evidence. For 10K/115K scale-up, a silent multi-hour run with no observable trajectory is a research-ops liability. CLAUDE.md `Goal override`: "never filter by implementation effort. Never take shortcut at the cost of compromising results." | m09c had no held-out validation loop, no intermediate Prec@K/mAP@K/Cycle@K checkpoints, no BWT monitor — only 3 scalar stage-end training losses + one final exported checkpoint. | Landed **mid-training probe infrastructure** surgery-only (user-approved scope). `src/utils/training.py`: `build_probe_clips` (pre-decodes val_1k once), `run_probe_eval` (bit-identical to m06 decision-gate: embed → L2-norm → FAISS IndexFlatL2 → `utils.bootstrap.per_clip_{prec,map,cycle}_at_k` + `bootstrap_ci` BCa), `run_probe_val_loss` (reuses `compute_jepa_loss` on probe clips), `compute_trajectory_stats` (BWT-proxy + monotonicity monitor). `src/m09c1_surgery_encoder.py`: every_n_steps cadence with `n_points=30` + forced stage-boundary anchors for true BWT. New yaml block `probe:` in `ch11_surgery.yaml` (mode-gated: sanity=off/poc=on/full=on, all keys required — no `.get(default)`). New outputs per run: `probe_trajectory.{png,pdf}` (33 pts × 3 metrics with CI bands), `m09_val_loss.{png,pdf}` (33 pts), `probe_history.jsonl`, scalar `bwt_prec_at_k` in `training_summary.json`. Measured overhead: 7.1 % on 2.7 h POC (33 probes × ~21 s each); scale-invariant because probe N stays fixed at 1000. AdaptiveBatchSizer wired (B35-compliant, reads `pipeline.yaml` gpu_memory_target). All `.get(key, default)` purged from wiring per CLAUDE.md `FAIL HARD`. Future (Step F): port identical infra to m09a / m09b for 3-arm apples-to-apples trajectory figure. | `src/utils/training.py`, `src/m09c1_surgery_encoder.py`, `configs/train/ch11_surgery.yaml`, `scripts/train_surgery.sh` |
| 64 | 2026-04-20 00:43 iter9 10K launch: m10 log line 29 showed `Output: /workspace/factorjepa/outputs/poc/m10_sam_segment` instead of runbook-documented `outputs/full/m10_sam_segment/`. `scripts/run_iter9_10k.sh` had 24× `outputs/full/...` hardcoded paths from earlier runbook draft; Verify A block (`cat outputs/full/m10_sam_segment/summary.json`) would have crashed post-m10 (~10 h GPU wasted on downstream failure). | `src/utils/config.py::get_module_output_dir(mod, args.subset, sanity=args.SANITY, poc=args.POC)` routes `--FULL + --subset <file>` → `outputs/poc/<mod>/` by convention (same quirk `scripts/train_surgery.sh --POC` exploits: `MODE_FLAG=--FULL, SUBSET_FLAG=--subset data/subset_10k.json, OUT_DIR=outputs/poc`). Script + runbook authored assuming `outputs/full/` — incorrect. | Live-patched at ~00:52 (during m10 run, before Verify A fired): `cp scripts/run_iter9_10k.sh /tmp/run_iter9_10k.sh.bak && sed -i 's\|outputs/full/\|outputs/poc/\|g' scripts/run_iter9_10k.sh`. Verified: 24 `outputs/full/` → 0; 24 `outputs/poc/`; `bash -n` clean. Zero GPU time lost because m10 was already writing to `outputs/poc/m10_sam_segment/` which downstream steps now look for. Not a code bug (config routing is by design) — runbook/script drift bug. **Preflight B22 added**: grep for `outputs/full/` in new `scripts/*.sh` when they also reference `--FULL --subset <file>` — these should use `outputs/poc/` for consistency with `get_module_output_dir` routing. | `scripts/run_iter9_10k.sh` |
| 65 | (Not an error — landed feature.) iter8 m11 materialized D_L + D_A as per-clip `.npy` to disk: 340 GB @ 10K, 1.7 TB @ 50K, 4 TB @ 115K. Gated the scale ladder behind a 500 GB → 3 TB → 5 TB vast.ai rent progression. Also: at-scale, m11 wall was pure-CPU `scipy.ndimage.gaussian_filter σ=15` × 10K clips = ~1 h at 10K, 10 h at 50K. | m11 + m09c were decoupled: m11 wrote `.npy`, m09c read `.npy`. Factor arrays lived on disk during the 3h20m training window — zero-value materialization. Training dataloader was a synchronous per-step loop (no PyTorch `DataLoader`, no workers, no prefetch). | Landed **streaming factor generation** (9 files, ~1014 LoC, 10/10 bitwise parity, 3 test tiers all green): `src/utils/factor_streaming.py` NEW (`stream_factor()` wraps m11's pure `make_layout_only`/`make_agent_only`, imports not duplicates; `tensor_from_factor_array()` shared normalization between legacy + streaming paths — guarantees bitwise parity). `src/utils/training.py`: new `StreamingFactorDataset(IterableDataset)` with per-worker `clip_keys[worker_id::num_workers]` sharding, seeded `np.random.default_rng((base_seed * 1_000_003) ^ (worker_id * 2_718_281))` per worker, per-worker `tarfile.open()` (fork-safe), yields `{"tensor", "factor_type", "clip_key"}` dicts. `build_streaming_indices(manifest_path, masks_dir, local_data)` one-shot TAR scan in main process → `(mp4_index, mask_index)`. `_streaming_worker_init` caps `torch.set_num_threads(1)` + PyAV log. `load_factor_clip` refactored to delegate to `tensor_from_factor_array(np.load(path), ...)` — legacy path stays bitwise-compatible. `src/m11_factor_datasets.py`: `--streaming` CLI flag short-circuits `_process_one_clip` via `agent_pixel_ratio`-only manifest entry for ~9,900 non-verify clips (only ~100 `select_verify_clips(seed=42)` get full processing for `plot_factor_per_clip`). `src/m09c1_surgery_encoder.py`: `merge_config_with_args` flattens `factor_streaming.{enabled, num_workers}`; per-stage `DataLoader(num_workers=16, persistent_workers, prefetch_factor=4, pin_memory, worker_init_fn=_streaming_worker_init)` vs legacy `FactorSampler`; `--[no-]factor-streaming` CLI override. `configs/train/ch11_surgery.yaml`: new `factor_streaming: {sanity:F, poc:F, full:T, num_workers:{0,4,16}, prefetch_factor:4, persistent_workers:T, pin_memory:T}` block. Tests: `scripts/tests_streaming/test_parity.py` (10/10 `np.array_equal` on iter8 POC .npy), `test_sanity_end_to_end.sh` (4/4 integration), `test_wall_time.py` (projected 4.77 h for 10K × 1 ep @ nw=16 — well under 10 h budget). **Net disk impact**: 10K 380 GB → 52 GB · 50K 1.7 TB → 150 GB · 115K 4 TB → 345 GB. Full scale-ladder fits on single 500 GB vast.ai instance. | `src/utils/factor_streaming.py`, `src/utils/training.py`, `src/m11_factor_datasets.py`, `src/m09c1_surgery_encoder.py`, `configs/train/ch11_surgery.yaml`, `scripts/tests_streaming/*` |
| 66 | (Not an error — methodology fix.) iter8 1K POC used `val_1k.json` (N=100 held-out after internal 90/10 split) for BOTH m09c probe (best-ckpt + BWT + early-stop) AND m06 decision gate. Best-ckpt selection across K=50 probes inflates reported Prec@K by σ × √(2 ln K) ≈ 0.77 × 2.8 ≈ **2.2 pp** (Taylor & Tibshirani 2015 selective-inference framework). This ≥ original 2 pp gate threshold → invalidates the reported Surgery − Frozen delta, reviewers flag as test-leakage. | Classic best-on-val selection bias. Probe touches val_1k ~50 times during training → picks max-Prec@K checkpoint → reports val_1k Prec@K of that max-checkpoint at test time. The reported number over-estimates true generalization by the max-of-K statistic. | 2026-04-20 00:26: one-shot disjoint split with seed=42 → `data/val_500.json` (probe role: best_ckpt / BWT / early-stop) + `data/test_500.json` (gate role: touched once at m06). Runbook + `scripts/run_iter9_10k.sh` + m09c command `--probe-subset data/val_500.json --probe-local-data data/val_1k_local --probe-tags data/val_1k_local/tags.json` updated; m05/m06 Steps D/E/F all use `--subset data/test_500.json`. CI widens ±1.5 pp → ±2.1 pp (N=500 vs N=1000, √2× wider) — still < 3 pp revised gate threshold. Gate thresholds rescaled: 2 pp → 3 pp pass, [0.5, 2) → [1, 3) marginal, < 0.5 → < 1 saturated. Paper draft must disclose split methodology explicitly. | `data/val_500.json`, `data/test_500.json`, `iter/iter9/runbook.md`, `scripts/run_iter9_10k.sh`, `iter/iter9/plan_training.md` (decision tree) |
| 67 | 2026-04-20 15:20 iter9 10K v3 run: Step B m11 completed factor-gen + 99 verify plots + top-20 curate cleanly, then crashed at `plot_factor_per_videoclip` line 482 with `FileNotFoundError: D_L/tier2__bhopal__drive__ZGKGG4OEoYE__ZGKGG4OEoYE-014.mp4.npy`. `set -euo pipefail` halted the pipeline before Verify B could run. | **Streaming refactor contract violation**: the 2026-04-19 m11 `--streaming` flag short-circuits .npy writes for ~9,900 non-verify clips but leaves all 10,000 manifest entries with `has_D_L=True`/`has_D_A=True`. `plot_factor_per_videoclip`'s top-20 ranker (`_score()`) sorts the full manifest, filters by `v.get("has_D_L")` (matches 9,933 entries), then `np.load`s the `.npy` of the first 20 — but under streaming only the ~99 verify clips have .npy files on disk. The ranker can (and did) pick clips whose manifest flag says "eligible" but whose disk state says "not materialized". I landed streaming for m11's factor-gen phase without auditing downstream plot consumers — 3 other plot funcs (`plot_factor_samples`, `plot_interaction_samples`, `plot_factor_per_clip`) are safe because they glob `dl_dir/*.npy` (read from disk first), only `plot_factor_per_videoclip` reads manifest first and loads from disk second. | Added `_has_materialized_dl(clip_key)` inner helper that `.exists()`-checks the .npy on disk; integrated into top-20 ranker via `[(k, v) for k, v in ranked if v.get("has_D_L") and _has_materialized_dl(k)][:top_n]`. Top-20 now draws from the ~99 verify clips under streaming, or all 9,933 has_D_L clips under legacy — same code path works both modes. Updated error message: "need at least D_L .npy materialized on disk" so future streaming failures at any scale surface the right diagnostic. Pre-flight check B11/B15-class static guard would not have caught this (it's a runtime contract between streaming-writer and plot-consumer); lesson: streaming refactors need a dedicated "all downstream consumers audited" checklist, added as backlog item to iter9/plan_TODO.md. | `src/m11_factor_datasets.py` |
| 68 | 2026-04-20 15:56 iter9 10K v4 Step C: `UnboundLocalError: local variable 'get_module_output_dir' referenced before assignment` at `src/m09c1_surgery_encoder.py:342` during `output_dir = get_module_output_dir(...)`. Fired BEFORE any training — crashed at the output-dir resolution step. | Local `from utils.config import get_module_output_dir` at line 452 (inside `train_surgery()` streaming-indices branch, guarded by `if streaming_enabled`) shadowed the module-top import at line 33 for the ENTIRE `train_surgery` function per Python scoping rules. Line 342 ran before line 452, but the compiler had already bound `get_module_output_dir` as a local → UnboundLocalError. Classic import-inside-function gotcha. | Removed the local re-import at line 452; added terse comment citing this entry. Followed by a session-wide audit: scanned all 6 files (m09a/b/c, m10, m10_legacy, m11) for indented imports; moved 24 non-ImportError-guarded imports to module top; kept only `try/except ImportError`-guarded `dotenv` at m10:29 (CLAUDE.md allowed exception). py_compile + ruff F,E9 green on all 6. | `src/m09c1_surgery_encoder.py`, `src/m09a1_pretrain_encoder.py`, `src/m09b_explora.py`, `src/m10_sam_segment.py`, `src/m10_sam_segment_legacy.py`, `src/m11_factor_datasets.py` |
| 70 | 2026-04-20 16:18 iter9 10K v6 Step B: m11 progress bar displayed `9504/10000` despite F1 fix shrinking `data/subset_10k.json` from 10000 → 9566. TAR reader correctly yielded only 9566 (subset-filtered), so m11 completed the actual work in <1 min — but then stalled for ~10 min on `clip_q.get(timeout=600)` at the end of Step B because `target=len(segments)=10000` never matched `n_done=9566`. Pbar mislabelled; factor_manifest ended up correct (9566 entries) but 10 min of idle wall-clock wasted per run. Same bug at 50K/115K scale would waste 10 min each. | `target = len(segments)` on line 898 hard-coded to the m10 mask cache count (`segments` = 10000 cached .npz files), ignoring that `subset_keys` (loaded at line 842 from `--subset data/subset_10k.json`) is the authoritative workload count when `--subset` is provided. Parallel bug: `verify_100_set` (line 869) selected 100 verify clips from `list(segments.keys())` (all 10K) instead of from the subset — under F1 shrink, ~4-5 of the 100 verify picks landed in the dropped 434 → those TAR-skipped → only ~95-96 `.npy` materialized instead of 100. | One fix, multiple sites: introduced `n_to_process = len(subset_keys) if subset_keys else len(segments)` at line 848 (single source of truth), then wired through (a) `Clips to process: {n_to_process}` print, (b) `make_pbar(total=n_to_process, ...)`, (c) streaming short-circuit count message `n_to_process - len(verify_100_set)`, (d) main loop `target = n_to_process`. Also fixed verify-pool selection: `verify_pool = list(subset_keys) if subset_keys else list(segments.keys())` so `select_verify_clips(verify_pool, n_target=100, seed=42)` always picks 100 clips that the TAR will actually yield. Python loads `.py` once at import — editing `src/m11_factor_datasets.py` while v6 was running in another process did NOT affect v6 (unlike bash-inode for shell scripts); fix applies to v7 onwards. | `src/m11_factor_datasets.py` |
| 69 | 2026-04-20 16:05 iter9 10K v5 Step C: `FATAL: permanent val_1k overlaps training manifest by 228 clips. Training set must be disjoint from val_1k (m00c normally guarantees this via seed).` m09c's assertion fired correctly AFTER Steps A (m10 cache-skip) + B (m11 --streaming → 10000-clip manifest). Training set HAD 434 clips in val_1k (228 in val_500 probe half + 206 in test_500 gate half). Test leak = 41% of test_500 → would have inflated surgical Δ metric by a research-invalidating amount. | `data/subset_10k.json` was built off-repo from `clip_durations.json` (source: `/Users/kapilwanaskar/...` Mac-side), NOT via an in-repo m00c-style disjoint generator — disjointness-from-val_1k invariant was never enforced. m09c's existing fail-loud assertion caught it (line 421-427) but only against `cfg["probe"]["subset"]` (val_500), so the 206 test_500 leak would have propagated silently into Steps D/E/F if the 228 val_500 overlap hadn't crashed the run first. Measured: `subset_10k ∩ val_500 = 228`, `subset_10k ∩ test_500 = 206`, `subset_10k ∩ val_1k = 434`; `val_500 ∩ test_500 = 0` (the split itself was clean — only the subset upstream was dirty). | **Option F1: filter subset at data layer.** Backup `data/subset_10k.json` → `data/subset_10k_with_leak.json.bak`, set-diff `subset_10k - val_1k` → 9566 disjoint clips, rewrite `data/subset_10k.json` with updated `n=9566` + `disjoint_from: data/val_1k.json` + `n_dropped_val1k_overlap: 434` metadata. Step B m11 `rm -rf` + re-runs on 9566 → regenerates factor_manifest.json clean. m10 cache for the 434 dropped clips becomes orphan (~600 MB, harmless). Alternate options rejected: F2 (m09c runtime filter) — NO DEFER violation, leaves dirty subset file; F3 (replace with 434 new clips) — 1.5-2h m10 re-run cost for cosmetic n=10000. Post-fix verify: `subset_10k ∩ {val_500, test_500, val_1k} = {0, 0, 0}`. Backlog: write an in-repo subset generator (`src/m00_subset.py`-style) that enforces `exclude_keys=val_1k.clip_keys` by contract so this class of leak cannot recur at 50K/115K. | `data/subset_10k.json`, `data/subset_10k_with_leak.json.bak` |

## iter10 additions (2026-04-21 → 2026-04-22 session)

| # | Error | Root Cause | Fix | File |
|---|---|---|---|---|
| 71 | 2026-04-21 20:58 iter10 v15a + 2026-04-22 00:04 iter10 v15b: `run_iter10_overnight.sh` logged `❌ v15a FAILED (exit=2)` / `❌ v15b FAILED (exit=2)` and archived outputs as `outputs_versioned/v15{a,b}_FAILED_*` despite m09c training, m05 embeds, m06 metrics, and m08b comparison all running cleanly to "=== COMPARISON COMPLETE ===". Spurious flagging masked real results: v15a Δ=0.00 pp + v15b Δ=−0.10 pp were correct but archived under FAILED prefix. Next `run_iter10_overnight.sh` invocation would continue to mis-flag identically. | `scripts/run_iter9_10k.sh:178` (pre-fix): `ls -lh outputs/full/m08b_compare/m08b_{encoder_comparison,radar,heatmap,spatial_temporal_bar,adaptation_ablation}.png 2>&1 \| head -10` — bash brace expansion produces 5 filenames. Under 2-encoder iter10 runs, `m08b_radar.png` is legitimately skipped by m08b (n_encoders<3 → min-max normalization degenerates to binary winner/loser) and `m08b_adaptation_ablation.png` is skipped (no vjepa_shuffled arm). `ls` fails non-zero on the 2 missing files → `set -o pipefail` catches → `set -e` aborts the script → outer orchestrator records exit=2 → `run_variant()` tags the tag as FAILED + archives. | Split verify into required + optional with explicit `for` loop: required list (`encoder_comparison,heatmap,spatial_temporal_bar` — always produced) runs `ls -lh` fail-loud; optional list (`radar, adaptation_ablation`) runs `for opt in ...; if [ -f $path ]; then ls -lh else echo SKIPPED fi` — no silent skips, no `\|\| true` (blocked by `fail-hard-research.sh` hook per CLAUDE.md). Post-fix verified: `bash -n` clean. v15a + v15b outputs re-labeled via `mv v15{a,b}_FAILED_* → v15{a,b}_*` in `outputs_versioned/`. Since bash holds inode on shell-script process launch, the fix applied cleanly to v15c's fresh invocation; v15b's in-flight process already held old inode and hit the same bug before archive — this is a known limitation (cannot live-patch a running bash script; wait for next invocation). | `scripts/run_iter9_10k.sh:177-187` |
| 72 | 2026-04-22 17:51 iter10 v3 paired_eval: `FATAL: Only 9,297/10,000 clips embedded (93%). Worker died before completion.` at frozen m05 → `set -euo pipefail` + `trap … ERR` on L31 aborted the whole 16h × 6-variant queue at L85. 703 clips (7%) failed PyAV decode; m05 exit=1 cascade-killed 15h of downstream GPU work that didn't need the missing 703. | Dual: **(a)** m05 `if final_count < total_clips * 0.95: sys.exit(1)` didn't distinguish "worker crashed early" (checkpoint suspect) from "worker tried all, 703 irrecoverable" (checkpoint complete); **(b)** shell orchestrator had no per-call guard around `python -u` invocations → any single exit=1 nuked all downstream variants, even though each variant has independent ckpt and doesn't consume m05's mid-flight state. | **(a)** Two-tier partial-tolerance in m05: new `exit_reason` var tracks stuck_clips vs worker_crash; post-loop gate is 80% emergency floor (FATAL always) + 95% normal floor (FATAL only on worker_crash; PARTIAL on stuck_clips → save partial `.npy` + `failed_clip_keys_<encoder>.json` + exit 0). **(b)** Added SKILL.md **B51** (codebase-agnostic): flag `.sh` with `set -e` + `trap … ERR` + ≥2 `python -u *.py` calls where none has a per-call guard (`\|\| {…}`, `set +e` window, or `if ! cmd`). Verified: v6 log L125 shows `⚠️ PARTIAL SUCCESS: 9,297/10,000 (93.0%) embedded; exit_reason=stuck_clips. Proceeding with partial .npy` → downstream m06 loaded 9,297 embeddings without m05 cascade-killing the queue. | `src/m05_vjepa_embed.py`, `.claude/skills/preflight/SKILL.md` |
| 73 | 2026-04-22 iter10 v15c_retry: yaml stage3 `mode_mixture: {L:0.15, A:0.15, I:0.70}` but log showed `StreamingFactorDataset: D_L=50%, D_A=50%` → recorded Δ=−0.13 pp (p=0.375, v15c_retry N=500) is a **mystery L/A re-balance, not the yaml recipe**. v15c ckpt scientifically invalidated; `outputs_versioned/v15c_m09c_surgery/` deleted. Clean re-run deferred (~14h + $11) in favor of iter11 50K pivot. | **Two-layer silent failure.** Upstream: `scripts/run_iter9_10k.sh:L56` m11 invocation hardcoded `--train-config configs/train/ch11_surgery.yaml` (base yaml) instead of the variant yaml passed to m09c → m11 read `interaction_mining.enabled: false` from base → segments.json `n_interactions=0` across all 9566 clips → m11 manifest has `has_D_I=False` everywhere. Same orchestrator L69-74 hardcodes `print(f'D_I tubes: … interaction_mining disabled')` + `assert sum(tubes) == 0` regardless of variant yaml → structurally ignored v15c's enable flag. Downstream: `src/utils/training.py::StreamingFactorDataset.__init__` L993 used `if self.clips_by_factor[fk]: append(factor, weight)` — a truthiness check = `.get(k, 0)` in disguise. When `D_I` list was empty, the loop silently dropped `I:0.70` → post-loop `total_w = 0.30` → renormalized `{L:0.15, A:0.15}` to `{L:0.50, A:0.50}`. Post-loop `if not self.factors: raise` fired only on TOTAL emptiness, insufficient for partial drops. **Same pattern present in legacy `FactorSampler.__init__` L798** (fixed together). | **(1)** `utils/training.py`: per-key `raise ValueError(f"mode_mixture requests {mode_key}={weight} but clips_by_factor[{fk}] is empty — silent renorm banned per CLAUDE.md §5; fix upstream m10 --interactions-only OR set {mode_key}=0 in yaml")` inside the loop (both `StreamingFactorDataset` AND `FactorSampler`). Post-loop raise now only catches the `all weights 0` config error. Print line now shows `REQUESTED={…} → ACTUAL={…}` for operator visibility. **(2)** `m09c_surgery.py`: new pre-flight block after `stages = surgery_cfg["stages"]` cross-checks each stage's non-zero `mode_mixture` keys vs `streaming_manifest` (or `factor_index`) `has_D_{L,A,I}` counts → raises `RuntimeError` up-front with per-factor count table before any stage loop starts. **(3)** Added SKILL.md **B50** (codebase-agnostic): flag class/fn iterating a `mixture\|policy\|recipe\|schedule\|weights\|blend\|ratios?` dict via `.items()` where body has `if <lookup>[key]: append()` without `else: raise` — catches the "yaml-requested key silently dropped" pattern repo-wide. Orchestrator fix TBD (`run_iter9_10k.sh` must pass `$TRAIN_CFG` to m11, L69-74 must branch `tubes==0` assert on `interaction_mining.enabled`) — deferred with iter11 50K pivot; see MEMORY. | `src/utils/training.py`, `src/m09c1_surgery_encoder.py`, `.claude/skills/preflight/SKILL.md` |
| 74 | 2026-04-22 18:24 iter10 v4 paired_eval: fresh frozen m05 started from `WORKER 1: embeddings 0 → 10,000` — v3's 9,297-clip `.m05_checkpoint_vjepa_2_1_frozen.npz` resume was LOST → +3.2h re-compute + ~$2.56 GPU sunk. | `scripts/run_paired_eval_10k.sh:L79` was `rm -rf outputs/full/m05_vjepa_embed` — blanket wipe nuked the hidden checkpoint .npz file before m05 launched, defeating m05's built-in `load_checkpoint` resume (L317). Intent was "clean slate" to drop stale .npy outputs; side-effect killed the 9,297 embeddings checkpointed by v3. | L79 replaced with: `mkdir -p outputs/full/m05_vjepa_embed && find outputs/full/m05_vjepa_embed -mindepth 1 ! -name '.m05_checkpoint_vjepa_2_1_frozen.npz' -exec rm -rf {} +` + added operator log line `🔁 Frozen checkpoint found → m05 will resume from N/10,000`. Verified v5/v6 resume chain: v5 18:35 resumed from 528; v6 18:45 resumed from 1,012 → progressive checkpoint preservation works. L139 per-variant surgical `rm -rf` kept as-is (all 6 variants shared `.m05_checkpoint_vjepa_2_1_surgical.npz` filename — reset between variants was REQUIRED there; separately addressed in #75). | `scripts/run_paired_eval_10k.sh:L79-92` |
| 75 | 2026-04-22 iter10 — latent correctness risk (no live incident yet): all 6 surgical variants (v10/v13/v14/v15a/v15b/v15c) keyed their m05 checkpoint only by encoder-name string → shared filename `.m05_checkpoint_vjepa_2_1_surgical.npz`. Per-variant `rm -rf outputs/full/m05_vjepa_embed` (paired_eval L139) masked the risk. If that wipe were removed or a workflow did sequential adapted-model m05 runs without it → e.g., v13 starting after v10 partially completed → v13 would silently resume from v10's checkpointed embeddings under v13's weights → per-clip embeddings from BOTH models mixed → garbage downstream. | `src/m05_vjepa_embed.py` L287/L521: `checkpoint_file = output_dir / f".m05_checkpoint{embed_suffix}.npz"` where `embed_suffix` depends only on `encoder_name` (constant `vjepa_2_1_surgical` for all 6 variants). No content/path hash. | Added `_checkpoint_fingerprint(model_path, is_adapted)` helper: `""` for frozen (backward-compat — frozen filename unchanged → currently-running v4/v5/v6 in-flight chain preserved), `"_<8-char sha256>"` of `abs_path\|st.st_size\|int(st.st_mtime)` for adapted. Threaded into both L287 (orchestrator branch) and L521 (worker branch). Verified 6 unique fingerprints (`v10: _746c2005, v13: _e00f4d40, v14: _d300312c, v15a: _ec346c56, v15b: _1509a754, v15c: _b693ab16`) + idempotent (same path → same hash). Also catches in-place weight replacement (mtime change → new fingerprint → no stale reuse). Per-variant L139 `rm -rf` now optional defensive cleanup rather than correctness requirement. | `src/m05_vjepa_embed.py` |
| 76 | 2026-04-22 — **(Not an error — preflight guard expansion.)** SKILL.md was 1096 lines (too verbose to load efficiently in context) and had no static checks for the iter10 incident classes (brittle-queue cascade #72, silent factor-drop #73). | SKILL.md accumulated 49 checks (A1-A3, B1-B49) over 8 iter sessions with heavy inline prose, blank-line padding, and per-Part `---` dividers → verbose but correct. Two new failure classes surfaced this session with no guard coverage. | Compacted SKILL.md from 1,096 → 474 lines (preserving all 49 pre-existing checks — no semantic loss) by: one-lining bash blocks via `python3 -c "…"`/`;`-joined statements, removing Part `---` dividers, collapsing blank lines between checks, trimming verbose `print()` messages. Added **B50** (selector silent-drop — see #73) + **B51** (brittle queue — see #72); both codebase-agnostic, AST/regex-based, <1s runtime. Total checks now 51 (A1-A3 + B1-B51). Trim script used: Python file-walker that strips `---` lines + collapses adjacent blanks; ran once, deleted. Frontmatter preserved. | `.claude/skills/preflight/SKILL.md` |
| 77 | 2026-04-22 20:45 iter10 v6 paired_eval: `FATAL: after filtering by clip_paths, 0/9297 embeddings matched a tag (via source_file). Check that tags.json covers the val-split clips.` at m06 step (L115 of shell) immediately after frozen m05 PARTIAL SUCCESS wrote 9,297-clip `.paths.npy`. Log shows `Loaded embeddings: (9297, 1664); Loaded tags: 1,000; Loaded clip paths: 9,297` → tags.json covered only 1,000 val_1k clips, zero overlap with the 9,297 eval_10k clips → paired_eval cascade-killed at m06 before any variant's surgical m05 could run. | `src/m06_faiss_metrics.py` tags-path resolution defaults to `data/val_1k_local/tags.json` (the iter8 hard-coded default used by the probe path) even when `--subset data/eval_10k.json` + `--encoder vjepa_2_1_frozen` indicates an eval_10k workflow. `data/eval_10k_local/tags.json` exists but m06 never routes to it. No `--tags` CLI flag exposed; no auto-derive from `--subset` stem. | ✅ LANDED (m06_faiss_metrics.py:L1095-1115). Auto-derive from `--subset` stem: `tags_path = Path(args.subset).parent / f"{Path(args.subset).stem}_local" / "tags.json"`; `--local-data` CLI wins if passed; fail-loud if neither resolves. Unblocked v7 paired_eval relaunch 2026-04-23 → iter10 v10/v13/v14/v15a/v15b all completed eval. | `src/m06_faiss_metrics.py:L1095-1115` |
| 78 | 2026-04-26 — **(Not an error — research-driven LR re-anchoring + dead-config cleanup.)** `surgery_2stage_noDI` v2 (5 epochs @ lr=1e-5, 380 steps, 27 probes) produced near-flat probe Prec@K (75.0 → 75.22, Δ +0.22 ≪ ±3.78 BCa CI_half at N=306) AND val_jepa still descending at run end (0.4946 → 0.4663, curve un-plateaued, plateau-patience=5 never fired). Classic under-training signature: model has more capacity to learn but LR/epochs starve it. Compounding footgun: `base_optimization.yaml:90 lr_schedule: constant` was a dead field — `build_scheduler` (utils/training.py:503-516) is unconditionally cosine-to-`min_lr` with no constant-vs-cosine branch. Zero Python readers verified via `grep -rn "lr_schedule" src/ configs/` (only yaml comment refs). Operator/reviewer reading the yaml would believe schedule is constant; actual behavior was always cosine. | **(a) LR anchor wrong**: prior `lr: 1.0e-5` was derived from Meta's *from-scratch* pretraining peak (6e-4 @ BS=1536 → 1.25e-5 BS-scaled to BS=32). But our regime is *transfer learning* on a frozen V-JEPA 2.1 ViT-G — the apt reference is Meta's *continual* training recipe `vjepa2/configs/train/vitg16/droid-256px-8f.yaml` (`lr: 4.25e-4` @ BS=256 → **5.3e-5 BS-scaled to BS=32**, rounded to 5e-5). 5× too conservative. **(b) max_epochs.full=5 too short**: SSL canon training continues until val-loss plateau; ours never plateaued. **(c) dead lr_schedule field**: residue from iter8 audit when `linear_decay` and `constant` were considered as alternatives but never implemented in build_scheduler — the field stayed in yaml as misleading documentation. | **5 yaml edits to `configs/train/base_optimization.yaml`** (inherited by all 4 variants via `extends`): `lr: 1.0e-5 → 5.0e-5`, `max_epochs.full: 5 → 15`, `warmup_cap_pct: 10 → 15` (more steps for optimizer to find stable basin at 5× LR — 171 steps vs 114 for 1140-step budget), `grad_clip: 10.0 → 1.0` (standard transformer-FT bound for short-budget runs; Meta's 10.0 is sized for 315-epoch absorbing capacity), `nan_tolerance: 3 → 2` (at 5× LR a NaN is divergence, not transient — 2 confirms vs 1 numeric blip). **Deleted dead `lr_schedule: constant` field** + updated surrounding comment to reflect that build_scheduler is unconditionally cosine. **Mirrored `max_epochs.full: 5 → 15` in `ch10_pretrain.yaml:27`** (overrides base). **SANITY pre-flight** (1-epoch FULL-mode at 5e-5, ~55 min, output isolated to `outputs/full/sanity_5e5_surgery_2stage_noDI/`) verified all 4 pass criteria: 0 NaN, max post-warmup grad=0.803 (zero clip events), val_jepa 0.4835 → 0.4693 (Δ−0.014 in 1 ep), Prec@K 75.05–75.22 (above 73.0 frozen floor). Killer head-to-head: 5e-5 in 1 epoch (val=0.4693) ≈ 1e-5 in 5 epochs (v2 final 0.4663) → **under-training hypothesis confirmed in real GPU time**. Sole active kill-switch (val-loss plateau, patience=5 probes ≈ 1 epoch at saves_per_epoch=5) backstops divergence. Prec@K-based kill_switch / prec_plateau / bwt remain disabled (operate below CI noise floor per `feedback_only_val_loss_early_stop.md`). | `configs/train/base_optimization.yaml`, `configs/train/ch10_pretrain.yaml` |
| 79 | 2026-04-27 — **(Audit class — pre-existing F821 silent-fallback chain caught by `ruff --select F` on a CPU lint pass.)** `m05b_baselines._finalize` referenced `args.cache_policy` but `args` is NOT a parameter of `_finalize` (3 callers all live in *other* functions: `generate_dinov2`, `generate_clip`, `generate_shuffled_vjepa`). Python free-var lookup would have raised `NameError` at the first invocation — undetected because the dinov2/clip/shuffled paths were dormant on `ultra_hard_3066_eval` until this session's baseline-sweep wiring (Dev #2). Same audit also surfaced `m09a_pretrain.select_ablation_winner(lambdas=None)` falling through to `load_merged_config(DEFAULT_MODEL_CONFIG, DEFAULT_TRAIN_CONFIG)` — both names undefined (would `NameError` at `--select-winner` invocation) — and 3 callers of `_finalize` using `getattr(args, "subset", None)` to silently default `subset_path` to `None`, which would skip the `verify_npy_matches_subset` integrity check (incident-class from #76 mtime-bug). | **Two compounding silent-fallback patterns**: **(1)** helper functions reading caller's `args.<key>` via *hoped*-lexical-scope (Python actually NameErrors, but the call site dormancy hid it for months); **(2)** `getattr(args, key, default_None)` swallowing missing CLI flags as `None` instead of crashing — both violate CLAUDE.md "No DEFAULT, FAIL LOUD" rule by deferring failure from "argparse-time" (immediately catchable, single-line stack trace) to "downstream silent garbage" (multi-h GPU run produces wrong .npy that passes shape checks). Same root taxonomy as #73 (`if dict[k]:` truthiness silent-drop) and #74 (`rm -rf` blanket wipe nuking checkpoint). | **(a)** `_finalize` now requires explicit `cache_policy: str, subset_path: str, label: str` positional kwargs — 7 params, 0 defaults. All 3 callers updated to pass `cache_policy=args.cache_policy, subset_path=args.subset, label="..."`. `getattr(args, "subset", None)` deleted everywhere (subset is `required=True` in argparse → always present, the getattr was paranoia). **(b)** `select_ablation_winner` now requires `lambdas: list` with non-empty assert; `__main__` block updated: `--select-winner` requires 3 positional args (`output_dir model_config.yaml train_config.yaml`); lambdas derived from `cfg["drift_control"]["ablation_lambdas"]` — `DEFAULT_*` undefined-name references eliminated. **(c)** Codified pattern in `src/CLAUDE.md` FAIL HARD bullet: `getattr(args, key, default)` is now banned alongside `\|\| true`, `\|\| continue`, bare `except: pass`. **(d)** Same audit pass cleared 2 cosmetic F-flags (m05b unused `get_output_dir` import; m05b f-string with no placeholders) + 2 unused stdlib imports (`copy`, `gc` in utils/training.py — leftover from technique-specific code that was extracted to shared utils per #49). Final ruff state: `All checks passed!` across all 5 modified files (utils/training.py, m05b_baselines.py, m09a_pretrain.py, m09b_explora.py, m09c_surgery.py). **Lesson**: CPU lint is cheap (~3s); dormant code paths only fail when activated, but ruff F821 catches the latent NameError at edit-time — wire ruff into `post-edit-lint.sh` PostToolUse hook with `--select F` (already done; this audit confirms the value of running on the *whole touched module*, not just the diff). | `src/m05b_baselines.py`, `src/m09a1_pretrain_encoder.py`, `src/utils/training.py`, `src/CLAUDE.md` |
| 80 | 2026-04-27 — `logs/run_train_explora_v10.log`: ExPLoRA v10 (15-epoch FULL @ lr=5e-5) tripped the 10-min producer-timeout safety net at step **791/1140** (~5h27m wall, 30% of training lost) after emitting **1,982,523** `WARN: decode failed (...): [Errno 2] No such file or directory: '/tmp/m09_a5sblfdp/<safe_key>.mp4.mp4'` lines. Surgery 2stage_noDI/3stage_DI runs on the **same** ultra_hard_3066 split + same 15ep@5e-5 recipe completed all 1140 steps with **0 decode failures** — divergence was the I/O pipeline, not the recipe. m09c uses `StreamingFactorDataset`/`DataLoader` with per-stage respawn; m09a/m09b use the shared `producer_thread` from `utils/training.py` with a single function-lifetime `tmp_dir`. | **Two stacked footguns** in `producer_thread` + `decode_video_bytes`. **(1) Long-lived shared `tmp_dir`** (`utils/training.py:135`): `tempfile.mkdtemp(prefix="m09_")` was created once at producer start and lived for the entire ~5h run. Across 16 DECODE_WORKERS × ~2k clips/epoch × 10 epochs = ~330k write/unlink cycles in a single `/tmp/m09_a5sblfdp/`. Past some threshold (likely tmpfs inode pressure, FS metadata accumulation, or external `systemd-tmpfiles` eviction on long-idle paths), every subsequent `open(tmp_path, "wb"); _load_av(tmp_path)` round-trip returned ENOENT at the read step. Once degraded, ~80% of every batch failed → producer queue starved → 10-min timeout fired. Surgery's per-stage DataLoader respawn (3 stages × ~5 epochs each → ~5 dir rotations) implicitly bounded the same state and never crossed the threshold. **(2) Doubled `.mp4.mp4`** (`utils/video_io.py:131`): HF stream clip keys like `tier1/.../abc-119.mp4` already end in `.mp4`; `safe_key.replace("/", "_")` preserves the suffix, then `f"{safe_key}.mp4"` re-appends → `abc-119.mp4.mp4`. Path is consistent across write+read in the same `decode_video_bytes` call so it's NOT the ENOENT cause, but it made the failure log misleading and obscured the real diagnosis for ~30 min. | **Option A — per-epoch `tmp_dir` rotation in `producer_thread`** (`utils/training.py:135-176`): renamed the function-lifetime dir to `m09_e1_*`; at the top of each epoch loop iteration (`epoch > 1` branch) `shutil.rmtree(tmp_dir, ignore_errors=True)` + `tempfile.mkdtemp(prefix=f"m09_e{epoch}_")`. Safe because `_decode_batch` blocks on `[(f.result(), k) for f, k ...]` so all decode pool futures are drained before the rotation point — no in-flight future references the about-to-be-deleted dir. Closure semantics: `_decode_batch` references `tmp_dir` as a free variable and Python LEGB resolves the latest binding at call time (verified via REPL test: rotated dir picked up by inner closure). Outermost `finally: shutil.rmtree(tmp_dir, ignore_errors=True)` retained as safety net for the LAST tmp_dir. Bounds tmp_dir lifetime to ~30 min / ~2k writes (one epoch on the 2452-clip train set) instead of ~5h / ~2M writes. **Option B (rejected)** would have wrapped the producer in an external per-epoch restart loop in m09b only — duplicates the epoch loop already inside `producer_thread` and introduces GPU-starvation gaps. **Option A is technique-agnostic** per the `utils/training.py` contract in CLAUDE.md (no `if args.explora` branches), so m09a inherits the same fix. **Option C — strip doubled `.mp4.mp4`** (`utils/video_io.py:130-138`): added `if safe_key.endswith(".mp4"): safe_key = safe_key[:-4]` before re-appending. Idempotent (keys without `.mp4` unaffected). Verified: `tier1/.../abc-119.mp4` and `tier1/.../abc-119` now both produce `/tmp/m09_e<N>_*/tier1_..._abc-119.mp4`. Defensive cleanup that doesn't fix the root cause but unblocks the diagnostic log. **3-check gate**: `ast.parse` ✓, `py_compile` ✓, `ruff --select F,E9` ✓ on both files. **Lesson**: long-lived shared tmpfs paths are a class-of-bug — anywhere a script holds a tmpfs handle for >1h on a busy /tmp, plan rotation cadence (per-epoch / per-stage / per-N-clips). Add B52 preflight: `grep -E "tempfile.mkdtemp.*producer\|long-lived.*tmpfs"` AST check that flags any `mkdtemp` outside an iteration boundary in a producer/streaming loop. | `src/utils/training.py:135-176`, `src/utils/video_io.py:130-138` |
| 82 | 2026-05-03 — `logs/run_src_m06d_v1.log:80-92`: m06d Stage 2 (V-JEPA features) crashed at the FIRST checkpoint save (~clip 512 / `streaming.checkpoint_every`, ~7-min wall) with `FileNotFoundError: '...m06d_features_train_ckpt.npz.tmp' -> '...m06d_features_train_ckpt.npz'`. Trace bottoms out at `frozen_features.py:190 os.replace(tmp, ckpt_file)` — the tmp file the rename references doesn't exist on disk. | **`np.savez` silent path mutation** (per [numpy docs](https://numpy.org/doc/stable/reference/generated/numpy.savez.html)): "If file is a string, the .npz extension will be appended to the filename if it is not already there." Code wrote `tmp = ckpt_file.with_suffix(".npz.tmp")` → tmp string ends in `.tmp` → numpy auto-appends `.npz` → ACTUAL file written is `....npz.tmp.npz`. Then `os.replace(tmp, ckpt_file)` looks for the original `....npz.tmp` (without numpy's appended suffix) and crashes. Same bug pattern present in `m06d_future_mse.py::_save_mse_ckpt`. iter13 m06d trio is the first place in the codebase using this `np.savez + os.replace` atomic-rename idiom — m05's earlier rename pattern uses a different path construction and dodges the bug. | **One-line fix per file** (`src/utils/frozen_features.py:188`, `src/m06d_future_mse.py:219`): `tmp = ckpt_file.with_suffix(".tmp.npz")` (suffix order swap so tmp already ends in `.npz` → numpy doesn't auto-append → os.replace finds the file). Added docstring + cross-reference to this entry. **Preflight guard**: added SKILL.md **B65** (codebase-agnostic regex): finds `np.savez(VAR) + os.replace(VAR, …)` pairs and flags when VAR's `with_suffix(...)` binding doesn't end in `.npz`. Self-tested: PASSes on both fixed files; FAILs on synthetic regression. **Lesson**: third-party-library silent path mutation is a class-of-bug B43/B46 don't catch — add a guard whenever a writer (np.savez, np.save, h5py.File, …) is followed by os.replace/os.rename of the same var. | `src/utils/frozen_features.py:188`, `src/m06d_future_mse.py:219`, `.claude/skills/preflight/SKILL.md` |
